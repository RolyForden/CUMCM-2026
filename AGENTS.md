# CUMCM Agent 运行规则

1. 开始工作前读取 `STATE.md` 和当前 `TASK`；需接续上一会话时再读 `SESSION_HANDOFF.md`。
2. `docs/AGENT_OS_DESIGN.md` 仅用于架构重设计或重新部署，日常任务不得默认加载。
3. 未收到人类明确命令前，不得切换项目阶段或分析真实竞赛题。
4. `main` 仅保存当前可信版本；任务在 branch 或 worktree 中完成，重大修改经检查和人类确认后才能合并。
5. `data/raw/` 不得修改；处理后数据写入 `data/processed/`，重要处理必须尽量可复现。
6. 复杂工作必须有简短 `tasks/TASK_xxx.md`；任务决定所需 Skill，不默认加载全部 Skill 或整个仓库。
7. 不确定的信息不得伪造；重大题意、假设、模型和冻结决策由人类确认。
8. 主要问题先建立简单 baseline，再比较不同模型方向；不得因已有结论而跳过验证。
9. 重要实验要记录方法、参数、种子、指标、代码、输出、结论和 commit；关键结果必须审查。
10. 任务完成或会话切换前更新 `SESSION_HANDOFF.md`；`STATE.md` 只保留当前状态，`DECISIONS.md` 只记重大决策。
11. 解题效率高于流程完整性；不得为形式化增加无明显收益的文件、流程或 Agent。
12. 凡涉及需要人类判断的内容，汇报要通俗简短：用大白话、少写字，只说与建模决策直接相关的信息，删掉无关的冗余汇报。
13. 论文使用 LaTeX 撰写：模板与编译都在 `paper/` 内完成（入口 `paper/数模通用模板.tex`，xelatex 编译），论文数字一律来自核算器复算的正式产物，不得手改结果数值。
