# 重要决策

## D001：最终选择 C 题

- **问题：** 在本科组、两名成员、约 1.5 天有效比赛时间的约束下，从候选题中确定最终投入方向。
- **决定：** 选择 C 题“微网与外部电网电力调控策略”。正确性优先的可选排序为 C > B > A；D、E 题不进入本科组候选。
- **理由：** C 题不仅数据完整、baseline 快，而且在模型、算法和结果三个层面都能独立验证：约束与能量账本可机械检查，优化结果可交叉求解，策略可在未参与决策的实际数据上顺序回放。A 的移动边界与物理真值缺失使“数值收敛但模型错误”难以排除；B 的模拟器、接口与有限正式测试构成关键路径风险。
- **证据：** `research/topic_selection_A.md`、`research/topic_selection_B.md`、`research/topic_selection_compare.md`。
- **授权：** 人类已明确委托本次同步得出最终选题结论。
- **复核：** 人类补充“建模和结果正确性为最大评分因素”后重新加权，结论仍为 C；B 因官方模拟反馈升为条件性第二，模拟器不可用时 A 才是实际后备。C 的正确性优势来自可审计和可回放，不是复杂模型名称。
- **比例因素：** 各赛道按相同比例授奖时，参赛人数与奖项数同步变化，选题热度不产生机械胜率优势。当前投票显示 C 最热门，只增加方案同质化风险；在缺少各赛道队伍质量分布的情况下，不据此改选 B 或 A。
- **状态：** 有效。项目已进入 `SOLVING`。

## D002：C 题 13 项 P0/P1/P2 口径冻结（2026-09-12，人类逐项拍板）

> 唯一事实基准：此后所有代码、结果、论文只按本条目口径走。13 项对应 `research/C_problem_map.md` 冻结清单与 `research/C_method_map.md` §1.5。每项给出问题、决定、理由、选错的风险。相关代码风险点：全题共用 144 槽位适配器、统一执行器与统一费用核算器。

### P0（8 项，不解决不能开始正式建模）

**P0-1 时间标签映射 → 起点对齐（映射 A）**
- **决定：** 输入 144 点标签 `00:10`…`0:00+1` 是各时段的**起点**：输入点 k 的值代表区间 `[标签_k, 标签_k+10min)` 的功率/电价，与结果模板区间列（`0:10-0:20`…`0:00+1-0:10+1`）逐槽一一对应，不做平移。
- **理由：** 官方模板首行 `0:10-0:20`、末行 `0:00+1-0:10+1` 与输入 144 点完全对齐，无 `0:00-0:10` 区间（`0:00` 是当天第一个时段的起点，题目只用次日 `0:00+1` 表示当天 24:00）。终点对齐需先平移一格且无法解释模板首行。
- **选错的风险：** 若实际是终点对齐，全部四问 144 时段都错位 10 分钟，Q1 锚点 35126.95 元不复现。
- **落实：** 建独立 144 槽位适配器（输入列→槽位→模板列），按槽位对列写入，禁止按时间字符串拼接；槽位映射恒定，四问共用。已按官方包逐表核验：输入 00:10 开列、末行 0:00+1，result1 首行 0:10-0:20 末行 0:00+1-0:10+1，result2/3/4 首列区间 0:10-0:20 且末尾有"全天购电量/全天购电费"两列（写作时用 146 列/147 列结构，不以本地缺表模板为准）。
- **落实（探针 2026-09-12 验证）：** 该映射的墙钟推论——槽 0 = [00:10,00:20)，槽 142 = [23:50,0:00+1)（跨日），槽 143 = [0:00+1,0:10+1)（完全落在次日）；官方输入无 `00:00` 列，当天 [00:00,00:10) 无输入数据、不参与调度（SOC 不变）。适配器已按此实现并与官方输入表头、result1 模板区间逐列对齐（探针 T1）。若人类复核后推翻该推论，需要同步修改槽位适配器、终端 SOC 约束、模板解释及对应测试，不能只改一处标签。

**P0-2 Q1 初始 SOC → 固定 6000 kWh**
- **决定：** Q1 初始 SOC 固定 E_0 = 6000 kWh，终端约束按"144 槽周期首尾相等"执行（E_144 = E_0，即次日 00:10 的 SOC 回到 6000）。
- **理由：** 附录 1 明确 2025-01-01 0:00 电量为 6000 kWh，是题面唯一给定初值；固定后与 Q2–Q4 从 1 月 1 日 6000 起的 SOC 演化完全一致。
- **选错的风险：** 不固定则模型自选有利初始库存（自由最优首尾 SOC=8550、费用 35118.60 元），与给定初值矛盾且 Q1 与 Q2 无法衔接。
- **敏感性（必做）：** E_0 ∈ {1200, 6000, 10800}。
- **探针记录（已由 D003 确认）：** 映射 A 下"24:00 储电量"= E_143（槽 142 末）。若把终端约束读作 E_143 = E_0，Q1 费用为 34881.454457 元，比主口径（E_144 = E_0，35126.948590 元）低 245.494132 元。人类已选择方案A，差异保留为敏感性对照；裁决记录见 D003 与 `research/C_time_boundary_decision.md`。

**P0-3 效率 90% → 充、放电各 90%（母线侧计量）**
- **决定：** η_c = η_d = 0.9。充电入库 = 母线侧充电量 × 0.9；向母线放电 `d` 时库存减少 `d/0.9`。**母线侧能量平衡** `grid_delivered + pv_used + discharge = load + charge`，且 `pv_curtail = pv_available − pv_used`；所有功率/电量计量均在母线侧。
- **理由：** "充放电效率为 90%" 最自然的读法；母线侧计量使能量平衡与结算（电价×母线侧购电）同侧，无隐藏折算。
- **选错的风险：** 若实际是往返 90%，全年费用低约 3.8%（Q1 锚点变 33801.50 元），结论会系统性偏乐观。
- **敏感性（必做）：** 往返口径 η_c = η_d = √0.9，另做总效率 ±5%。

**P0-4 弃光/反送 → 允许弃光、禁止反送**
- **决定：** 光伏富余且电池已满时允许弃光（`pv_curtail ≥ 0`），不允许向外网反送（`grid_delivered ≥ 0`）。弃光不设自由变量，而由 `pv_available − pv_used` 计算。
- **理由：** 题面未给售电规则；不允许弃光则富余且电池满时模型人为不可行。
- **选错的风险：** 禁止弃光会在高光伏日出现不可行；允许反送会引入题面没有的收益项。
- **敏感性（必做）：** 弃光开关（检查全年可行天数）。

**P0-5 Q2 信息集 + Q4 电价可知性 → 严格因果；1 月为 warm-up；Q4 主口径电价不可知**
- **决定：** ① 每天 0:00 计划只用 0:00 及之前已发布的信息（历史实测、当时已发布的预报），**当天实际负荷/PV 未知**；负荷预测只能用历史数据构造。② 1 月为 warm-up 窗口（预热预测器 + 按规则推进 SOC），2–12 月为正式回测输出期。③ Q4 主口径按**当天未来电价在决策时不可知**：用决策时可获得的历史电价预测（如 D-7/同星期日基线），结算仍按附件 4 真实电价；真实电价 oracle 只作不可执行下界对照。
- **理由：** 附件给出全年真实数据不代表决策时已知；否则 Q2 紧急购电被人为压成零、Q3 预测更新失去意义。Q4 电价可知性题面未明说，因果口径是可执行且能展示"可执行性代价"的主口径。
- **选错的风险：** 使用未来真值 = 信息泄漏，Q2 零紧急购电或异常低成本是典型泄漏特征，直接判错。
- **敏感性（必做）：** 1 月纳入调度期的对照；Q4 真实电价 oracle 并列展示。**结果模板只提交一个主口径。**

**P0-6 跨日 SOC 与冷启动 → 连续传递**
- **决定：** 前一天末 SOC 传到下一天（E_0(d+1) = E_144(d)）；1 月 1 日 0:00 从 6000 kWh 起，1 月按同规则演化到 2 月 1 日，2 月 1 日**不重置**。
- **理由：** 每天重置 6000 会虚构能量、破坏物理连续性（方法地图失败模式 #3/#20）。
- **选错的风险：** 逐日复位会系统性低估/扭曲跨日储能价值。
- **敏感性（必做）：** 逐日复位 6000 作为失败模式对照。

**P0-7 电池实时重调 → 按计划执行，不做实时重调**
- **决定：** 实际负荷/PV 揭示后，电池**严格按 0:00（或最近一次更新）计划执行**，不因实际值偏离而临时改充放电；缺口全部走紧急购电（5 倍）。每天 0:00 的 LP 自带安全裕度（裕度策略用 0.8 分位等候选在 walk-forward 上竞标，见 P0 之外的 Q2 方法选择）。
- **理由：** 与题面"除紧急购电费用外，其他时间段按计划购电量计费"的字面读法一致；费用账本最简（计划 + 紧急），Q2/Q3 执行器统一。
- **选错的风险：** 若允许实时重调，缺口与费用结构都会不同；本决定下"电池有电仍买紧急电"是正确行为而非错误（论文必须解释）。
- **敏感性（必做）：** 允许实时重调的另一规则作为对照口径，不入主结果。

**P0-8 Q3 调整结算 → 替代价式，相对上一版**
- **决定：** 调整结算为**替代价**（重叠部分不重复计费）：每时段 `C_t = p_t·min(q_old, q_new) + 0.5p_t·(q_old − q_new)_+ + 1.5p_t·(q_new − q_old)_+`。**调整基准为上一版计划**：q⁶ 相对 q⁰、q¹² 相对 q⁶、q¹⁸ 相对 q¹²。
- **理由：** "计划高于调整的部分按 50% 违约电价"的最自然读法是被替换部分按 50% 结算，不是额外罚金；相对上一版与滚动重优化语义一致。下调后实际总费用可以低于原计划费用。
- **选错的风险：** 额外罚金/相对初始口径费用会显著更高（本地资料中 0.13%–3.18% 的差异正是口径不同造成，非算法优劣）；重复计费是硬错误。
- **敏感性（必做）：** 相对初始计划基准的对照，仅作敏感性。

### P1（3 项，建模过程中必须冻结）

**P1-1 "每天电价相同"声明 → 日内曲线每天重复**
- **决定：** 指附件 1 的 144 点日内电价曲线（0.371–1.395 元/kWh）每天重复，不是一天内恒价。论文必须显式声明此读法。
- **理由：** 附件 1 实测 144 点电价明显波动；"每天相同"只能指曲线逐日重复。
- **选错的风险：** 日内恒价会抹掉日内套利结构，Q1 储能调度失去意义。

**P1-2 预报映射与视野 → 零阶保持主口径**
- **决定：** 附件 3 的"预报 k 小时"= 发布时刻后第 k 个小时的整点光伏预报（已核验：首日 0:00 发布的首小时预报为 0，凌晨无光照属正常）；整点 → 10 分钟粒度用**零阶保持**（预报 k 小时值作用于发布后第 k 个小时内全部 6 个 10 分钟时段），分段线性/PCHIP 作敏感性，以因果运行费用选优（禁止用未来实际光伏作插值锚点）。跨日 24h 视野**不截断**：跨日有效时段保留，只用于重优化未执行时段。附件 3 日期列为合并单元格（365 天 × 4 发布时点），预处理必须前向填充。
- **理由：** "整点预报"语义下零阶保持最忠实；插值方案由费用证据决定，不凭视觉平滑。
- **选错的风险：** 错位/截断/拼接后发预测会直接改变 Q3 可用信息（失败模式 #15/#16）。
- **敏感性（必做）：** 分段线性、PCHIP 对照。

**P1-3 "是否需要其他时刻预报"判据 → 费用+紧急+稳定性**
- **决定：** 用扣除调整成本后的总费用、紧急购电量和策略稳定性（调整幅度/次数、SOC 轨迹稳健性）共同判断，不以 PV 预测误差（RMSE/MAE）单独决定；论文以更新时点逐级消融（0:00 only → +6 → +12 → +18）定量回答。
- **理由：** 预测更准不等于策略更好；业务价值必须用统一核算器下的实际费用证明。
- **选错的风险：** 只报预测误差无法回答"是否需要引入其他时刻的预报"这一题面明确要求。

### P2（1 项，输出阶段确认）

**P2-1 结果模板完整性 → 展开全部 334 天 + 补齐缺表**
- **决定：** result2/result3/result4-2/result4-3 的省略号行展开全部 334 天（2025-02-01 至 12-31），保持原工作表与列结构；写入按槽位对列，不按标签字符串拼接。**模板缺失的工作表按题面补齐**（见下方"数据源核实"），官方模板原有的隐藏表（Sheet1/Sheet2 宏信息）保持原样。result2/3/4 表格尾列含"全天购电量/全天购电费"两列，必须填写。
- **理由：** 题面要求保存完整策略；缺表不填 = 题面要求未完成。
- **选错的风险：** 只填模板已有行会被判不完整。

### 数据源核实（2026-09-12 官方包逐表核验，属 D002 一并冻结）

- **官方包本机副本缺表：** `CUMCM2026Problems/C题/附件/附件2.xlsm` 只有"小区负载"表，缺题面要求的"光伏发电实际功率"表；附件 5 的 result1/result2/result4-2/result4-3 缺"充放电量"与"紧急购电"工作表（附件 3 与 result3 完整）。
- **决定：** 用 `reference/chapt1/26国赛C题成品论文3/2026Cnew/data/附件2.xlsx` 的光伏表（52560 值，min 0、max 10216.2 kW，无缺失无负值）作为替补数据源，与官方包负载表逐值一致（53070 值 mismatch=0）；附件 1/3/4 官方包与副本逐值一致。正式提交结果前如能重新从官网下载原包，比对 SHA-256 后替换；模板补齐时以题面（`tmp/pdfs/c-problem-review/C题.txt`）为准，保持官方模板原有表结构与格式。
- **数据固定口径：** Δt = 1/6 h，功率 × Δt 转 kWh；充/放电量上限 5000/6 = 833.333... kWh/格；SOC ∈ [1200, 10800]；电价/功率计量在母线侧。求解器为配置项（HiGHS 双引擎对拍），不入冻结清单。

### 不属本条目、但必须遵守的既有约束

- Q1 数值锚点（条件性回归值）：本口径下 Q1 应为 **35126.948589 元**、能量平衡最大残差 ~1e-13（已在冻结口径下独立复算），探针通过标准之一。
- 优化器目标值仅供对照，论文数字一律以执行器+核算器复算为准；Q1–Q4 共用同一核算器。

## D002 补充：探针整改后的实施状态与待裁决项（2026-09-12）

> `2250523` 的 41/41 仅保留为整改前基线。交叉验证发现结构缺陷后，代码在提交 `7464ccca35ad5a0eb454d0441fb6e051264e1582` 完成整改：主探针 48/48、独立交叉验证 39/39。证据见 `research/C_probe_cross_validation.md`。以下只同步实施事实，不改变上方人类冻结口径；N4已由D003裁决，D004-D006为N1–N3复审前裁决稿，冲突处以后续D007为准。

### 已落实的结构性修正（TASK_C_probe_model_corrections.md）

1. **弃光只能来自光伏**：内核以 `pv_used ≤ pv_available` 表达，`pv_curtail = pv_available − pv_used` 由恒等式推导；不再存在自由 `w`。T4的准确结论是：弃光出口已关闭，连续LP的电池循环假出口由审计捕获，独立互斥MILP确认无合法出口时不可行。
2. **Q1 购电语义**：`grid_contract = grid_delivered`，`grid_unused = 0`，`grid_emergency = 0`；目标只算 `Σ price·grid_contract`，Q2 规则不混入。
3. **计划版本不可变**：`src/core/executor.py` 的 `PlanRecord` 按 target_slot/issue_time/plan_version/previous_version/quantity/increase/decrease 追加新版本，不覆盖旧版本；Q3 多次结算以D007修订后的D005口径执行。
4. **因果数据接口**：已拆分 `build_q1_inputs`、`history_actuals_as_of`、`replay_actual_day`、`forecast_vintage_as_of`。历史实际值按 `observed_at ≤ decision_time` 截断，预报按 `issue_time ≤ decision_time` 截断；附件发布日期不再冒充观测时刻。
5. **按日期读取**：全年宽表按标准化日期唯一定位，缺失、重复、非144个数值均报错，不再固定读取第2行。
6. **独立核算**：空记录、SOC断链、重复/跳号/逆序槽位、同时充放电和Q1购电语义错误均被拒绝；核算器不调用LP状态更新函数。
7. **可行基线**：探针已用“无储能基线”替换末槽直接补费用的旧规则法，基线经过同一执行器和核算器，且费用不低于LP。
8. **官方模板回写**：复制官方 `result1.xlsm` 到系统临时目录，以 `keep_vba=True` 只写目标单元格并回读；已验证144槽、工作表顺序/隐藏状态、非目标单元格和VBA容器保持。

### 原待裁决项及复审状态

- **N1 Q2 多买电去向（D004为复审前稿，D007修订后条件冻结）**：合同电按计划照常计费；未实际进入母线的部分记为 `grid_unused = grid_contract − grid_delivered`，不进入能量平衡；计划放电过剩处理见D007。
- **N2 Q3 多次调整累计结算（D005为复审前稿，D007修订后条件冻结）**：采用相对上一版的逐次现金流，电价 1 时 `100 -> 80 -> 100` 累计费用为 120；目标槽电价、防重复计费与Q4防泄漏见D007。
- **N3 预测首小时映射与冷启动（D006已被D007撤回关键结论）**：附件3原始值先按 `valid_time = issue_time + lead_hour` 点预测保存；点到10分钟展开未裁决。
- **N4（已裁决，见 D003）**：采用映射A和 `E_144=E_0`，不再阻塞Q1；`E_143=E_0`只作敏感性对照。

D007后当前闸门：N1/N2只允许先做合成反例和单元测试；Q2三天窗口需N1反例通过且Q2光伏预测口径明确；Q3纯结算账本测试需N2反例通过；Q3预测驱动滚动窗口需N3重裁；Q2–Q4全年正式运行继续禁止。

## D003：N4 时间边界选择方案A（2026-09-12，人类确认）

- **问题：** 附件与result1模板的144槽覆盖当天00:10至次日00:10，而题面“0:00与24:00储电量相同”的墙钟字面对应次日00:00；必须确定终端SOC落在 `E_143` 还是 `E_144`。
- **决定：** 选择方案A。保持起点对齐：slot 0=`[当天00:10,00:20)`，slot 142=`[当天23:50,次日00:00)`，slot 143=`[次日00:00,00:10)`；当天`[00:00,00:10)`无输入且假定无动作。完整执行附件144槽后约束 `E_144=E_0=6000 kWh`。
- **主结果：** Q1费用采用 `35126.948590` 元；该值现为已冻结方案A下的正式主口径结果。
- **对照：** `E_143=E_0` 的 `34881.454457` 元只作时间边界敏感性，不写入正式结果模板。
- **理由：** 方案A完整覆盖官方输入和模板的144槽，并避免最后一槽在没有终端库存价值时无约束消耗电池；Q2–Q4继续按同一144槽周期传递 `E_144(d)=E_0(d+1)`。
- **影响：** N4解除；允许生成Q1正式结果并撰写其墙钟解释。D003形成时N1–N3仍未裁决；D004-D006曾给出复审前裁决稿，后续由D007修订。Q2–Q4全年正式求解继续禁止。
- **实施状态（2026-09-12）：** Q1 正式结果已按方案A生成并通过当前仓库可复现验证：生成期审计（16 项）+ 独立重开核验（8 项）均通过；`src/validate_q1.py` 完成逐槽复算、导出一致性、显式互斥 MILP、参数敏感性和 60 次输入扰动验证。产物 `outputs/q1_scheme_a/`（result1.xlsm、q1_dispatch.csv、q1_paper_tables.json、q1_audit.json）；验证报告 `research/C_q1_validation.md`，机器结果 `outputs/q1_validation/q1_validation_summary.json`。Q1 正文已写入 `paper/数模通用模板.tex`（latex 模板已从已验证的外部模板迁入 `paper/`），编译 14 页无错误。当前仓库不存在 `src/verify_q1_result.py` 及 `experiments/q1_cross_validation_*`，不将“32/32独立交叉验证”列为当前证据。此为实施事实记录，不改动本条目决策面。

## D004：N1 Q2合同余电处理（2026-09-12，复审前裁决稿，已由D007部分取代）

- **问题：** Q2 为降低紧急购电风险会保守购电。如果计划购电量高于实际需要，多买的合同电不能靠弃光或电池循环假消耗，必须明确账本去向。
- **决定：** 采用“合同照常计费，未实际供电部分记为未用合同电”。`grid_contract` 是计划合同量，按正常电价计费；`grid_delivered` 是实际进入母线的合同电；`grid_unused = grid_contract - grid_delivered` 表示付了钱但未用的合同电，不进入母线能量平衡。合同不足时，缺口记为 `grid_emergency`，按5倍电价计费。
- **能量账：** `grid_delivered + grid_emergency + pv_used + discharge = load_actual + charge`。`grid_unused` 不在能量平衡内。
- **费用账：** `cost = price * grid_contract + 5 * price * grid_emergency`。`grid_unused` 不额外收费，因为已包含在合同购电费中。
- **理由：** 该口径既保留保守计划的费用代价，又不引入题面没有的余电出口；多买电不会被伪装成弃光或电池损耗。
- **实施材料：** `research/C_q2_surplus_contract_decision.md`。
- **影响：** N1解除；允许实现 Q2 执行器小窗口。仍禁止 Q2 全年正式运行，必须先跑 3天小窗口并由核算器审计。
- **复审状态：** 本条对合同余电的账本仍保留，但“允许实现 Q2 执行器小窗口”的范围被D007收窄。计划放电造成实际过剩、审计字段和光伏优先敏感性以D007为准。

## D005：N2 Q3多次调整结算（2026-09-12，复审前裁决稿，已由D007部分取代）

- **问题：** Q3 在 0:00、6:00、12:00、18:00 多次更新购电计划。单次替代价公式不能直接替代多次现金流，否则会出现重复收费或漏收费。
- **决定：** 采用“逐次现金流，相对上一版计划”。初始计划 `q0` 先按 `price*q0` 计费；每次从 `q_prev` 调到 `q_new`，上调部分按 `1.5*price` 增购，下调部分冲回 `0.5*price`。公式为 `price*q0 + Σ(1.5*price*(q_new-q_prev)_+ - 0.5*price*(q_prev-q_new)_+)`，紧急购电另按 `5*price*grid_emergency` 计费。
- **手算锚点：** 电价1时，`100 -> 80 -> 100` 的计划相关费用为 `100 - 10 + 30 = 120`。
- **输出语义：** `计划购电量` 写0:00初始计划；`调整购电量` 写该槽位最终生效计划；内部日志保存每次版本，费用核算必须读版本日志，不能只看最终表。
- **理由：** 与D002“相对上一版计划”的语义一致，能真实反映下调后再上调的代价。
- **实施材料：** `research/C_q3_settlement_decision.md`。
- **影响：** N2解除；允许实现 Q3 单日滚动账本与反例测试。仍禁止 Q3 全年正式运行。
- **复审状态：** 本条的逐次现金流和 `100 -> 80 -> 100 = 120` 锚点保留；目标槽电价、防重复计费和Q4价格信息集以D007为准。Q3纯结算账本测试需N2反例通过后才允许。

## D006：N3 附件3预测映射与冷启动（2026-09-12，复审前裁决稿，关键结论已由D007撤回）

- **问题：** 附件3的“预报1小时”必须映射到明确的10分钟槽位；1月 warm-up 到2月1日的 SOC 也必须闭合。
- **决定：** `预报h小时` 覆盖 `[issue_time + (h-1)h, issue_time + h h)`，因此 `预报1小时` 覆盖发布后立即开始的第一个小时。小时预测展开到10分钟时采用零阶保持。更新只影响 `slot.interval_start >= issue_time` 的尚未执行槽位。
- **方案A衔接：** 0:00发布的 `预报1小时` 覆盖 `[00:00,01:00)`；当天 `[00:00,00:10)` 没有结果槽，不补造；slot0 `[00:10,00:20)` 使用该小时预测。
- **冷启动：** 2025-01-01 00:00 从 SOC=6000 kWh 开始，1月按同一执行器顺序 warm-up，2月1日初始 SOC 取1月31日 `E_144`，不重置。负荷预测先用因果基线：优先D-7，不足7天用D-1，D-1也不存在时用附件1典型日作为Jan1先验。
- **因果约束：** 所有训练实际值满足 `observed_at <= decision_time`，所有预报满足 `issue_time <= decision_time`。
- **实施材料：** `research/C_forecast_mapping_decision.md`。
- **影响：** N3解除；允许实现附件3预测展开适配器与 Q3 单日滚动窗口。仍禁止 Q2-Q4 全年正式运行。
- **复审状态：** D007撤回本条“预报1小时覆盖发布后立即一小时”和“N3解除”的结论。附件3原始值先按 `valid_time = issue_time + lead_hour` 点预测保存；点到10分钟展开未裁决，重裁前不得实现正式Q3预测适配器。

## D007：N1-N3复审决定（2026-09-12，人类授权按最新审阅修订）

- **问题：** D004-D006给出了N1-N3裁决稿，但独立审查发现：N1缺计划放电过剩处理，N2缺目标槽电价与防重复计费，N3首小时映射与附件数据强烈冲突，且每日 `E144=E0` 不能直接升格为Q2/Q3全年正式裁决。
- **审查证据：** `research/C_N1_N3_decision_review.md`。附件3更支持 `valid_time = issue_time + lead_hour` 点预测语义：全年点对齐MAE 186.82 kW，D006前一小时区间均值MAE 446.53 kW；lead=1分别为18.69 kW与307.01 kW。
- **D007/N1 决定：** 合同余电账本沿用D004，但新增“计划优先”执行假设：先弃光，再减少合同交付形成 `grid_unused`，最后在仍有实际过剩时削减计划放电。执行日志必须记录 `discharge_planned`、`discharge_actual`、`discharge_shortfall = discharge_planned - discharge_actual`；`discharge_clipped` 只能作为事件标志。光伏优先口径作为敏感性对照，不进入主线。
- **D007/N2 决定：** Q3调整现金流使用目标交付槽电价。计划费用唯一来源为版本现金流 `price[target_slot] * q0 + adjustments`，不得再对最终合同量 `q_final` 重复收普通合同费。Q4中优化器只能使用决策时可得的 `price_forecast[target_slot]`，核算器使用 `price_actual[target_slot]`，不得把目标槽电价解释成可读取未来真实价格。
- **D007/N3 决定：** 撤回D006的“预报1小时覆盖发布后立即一小时”。附件3原始24列先保存为点预测：`valid_time = issue_time + lead_hour`。点预测如何展开到10分钟槽位另行裁决；重裁前不得实现正式Q3预测适配器，也不得运行预测驱动Q3滚动窗口。
- **D007/终端SOC 决定：** Q2/Q3探针可临时要求计划层 `E144_plan = E0_actual`，只为防止有限窗口末端耗空。执行层 `E144_actual` 不强制等于 `E0_actual`，下一日必须从实际 `E144_actual` 继续。全年正式主模型必须在滚动视野、终端价值、每日回初值三者间做费用、可行率和SOC轨迹对照后再定。
- **当前闸门：** N1/N2文档口径按本条修订后冻结；允许N1/N2合成反例和单元测试。Q2三天窗口需N1反例通过且Q2光伏预测口径明确后才允许。Q3纯结算账本测试需N2反例通过后才允许。Q3预测驱动滚动窗口需N3重裁后才允许。Q2-Q4全年运行继续禁止。
- **实施状态（2026-09-12）：** D007 前置反例与只读验证已完成，`src/d007_prewindow_tests.py` 17/17 通过，机器结果见 `experiments/d007_prewindow_results.json`，报告见 `research/C_D007_prewindow_report.md`。Q3 纯合成结算账本测试也已完成，`src/q3_ledger_synthetic_tests.py` 9/9 通过，机器结果见 `experiments/q3_ledger_synthetic_results.json`，报告见 `research/C_Q3_pure_ledger_report.md`。Q2 三天窗口仍需 N1 接口测试与 Q2 光伏预测口径；Q3 预测驱动滚动窗口仍需 N3 点到10分钟展开重裁。

## D008：第二问库存安全削减（2026-09-12，人类选择方案A）

- **问题：** 实际负荷低于预测时，计划放电可能被削减，导致实际库存高于计划；后续继续执行原充电量可能超过10800 kWh上限。
- **决定：** 允许执行器仅为保持电池物理边界而临时削减充电量。记录 `charge_planned`、`charge_actual`、`charge_shortfall = charge_planned - charge_actual`；`charge_clipped` 只作事件标志。实际充电不得超过计划充电，实际库存不得越界。
- **理由：** 电池容量是硬约束；将整个策略判失败会使普通预测误差造成大量不可执行日。安全削减只在边界触发，且全部留痕，可在核算器中独立复查。
- **费用与余电：** 少充后不再需要的合同电进入 `grid_unused`，光伏按D007规则弃光；正常合同电仍按计划购电量收费。
- **影响：** 解除第二问执行器的库存上限处理歧义，允许继续做连续窗口；不改变第三问或第四问规则。

## D009：第二问时间方案A的零点衔接（2026-09-12，人类选择方案A）

- **问题：** 时间方案A中，前一天计划的最后一槽为次日00:00至00:10，但新一天计划要求在00:00制定；此时00:10真实库存尚不可知。
- **决定：** 00:00至00:10继续执行前一天已经制定的最后一槽计划，新一天计划从00:10开始。在00:00制定新计划时，只用当时已知的实际库存和旧计划已安排的充放电动作估计00:10库存；不得读取该十分钟尚未发生的实际负荷或光伏。
- **执行偏差：** 00:10得到真实库存后，新计划仍按原版本执行；估计与实际的偏差由D007放电削减和D008充电安全削减吸收，下一日继续传递实际库存。
- **理由：** 保留题目“每天00:00制定计划”和已冻结的144槽时间方案，同时避免提前读取00:10才完整观测的数据。
- **影响：** 第二问顺序回放必须在每个午夜先制定次日计划，再执行旧计划最后一槽；不得在离线回放中颠倒顺序。

## D010：第二问一月冷启动（2026-09-12，人类选择方案A）

- **问题：** 2025年1月1日没有昨天或上周历史，但必须从6000 kWh开始推进实际库存，才能得到2月1日真实的起始状态。
- **决定：** 1月1日使用附件1的典型日负荷和光伏预测作为先验并正常运行储能。1月2日至7日，每个槽位优先使用决策时已经完整观测到的同槽历史平均；没有历史的槽继续使用附件1先验。1月8日起启用历史预测候选。
- **因果边界：** 所有历史来源必须满足 `observed_at <= decision_time`。按方案A，前一天末槽在当天00:10才完整观测，因此当天00:00尚不可用时必须回退附件1，不能偷看。
- **影响：** 一月按相同执行器推进实际库存，2月1日不得重置6000 kWh。最终历史预测候选仍须用过去数据选择并用未来时段评价。

## D011：第二问全年终端库存口径（2026-09-12，人类选择滚动7天视野）

- **问题：** D007 规定全年正式主模型必须在"每日回初值、滚动多日视野、终端库存价值"三者间对照后再定；三天探针使用的"每日计划首尾库存相等"只是小窗口临时口径。
- **对照证据：** `research/C_Q2_terminal_comparison_report.md`、`experiments/q2_terminal_comparison_results.json`。全年334天、七天前同刻点预测、独立核算全部通过（不可行天数均为0）。
- **对照结果：**
  - `plan-eq`（每日回初值）：总费用 16,959,232 元，334天日末库存全部钉死在10800 kWh上限，正常购电费最高，电池失去跨日调节能力，为病态口径。
  - `terminal-value-θ`：θ=0/0.4 时库存被抽到1324 kWh、69天贴下限；θ=1.2 时167天顶回上限、费用最高。对 θ 敏感，不是稳健口径。
  - `rolling-3d/7d`：费用最低（约16.89万×100元），日末库存稳定在中段（rolling-7d 中位数8663 kWh），不贴上下限，跨日调节能力完整，且不需要人为标定终端价值参数。
- **决定：** 全年正式回放采用**滚动7天视野（rolling-7d）**。期末回到起始库存，只执行第一天；视野内未来7天的负荷/光伏用同一套因果预测生成。
- **理由：** 滚动视野用"未来几天要回来"替代对终端库存的人为定价，避开 θ 标定问题；在费用、可行率、库存轨迹三项上同时最好。7天覆盖一个完整周循环，包含周末与工作日差异。
- **影响：** 允许在 rolling-7d 口径下运行正式全年顺序回放并生成 result2。终端价值的 θ 敏感性只在主线完成后作增强对照，不进入主结果。
- **附带发现（待全年复核）：** 2月至4月窗口、rolling-3d 下，点预测总费用低于 0.7/0.8/0.9 分位保守候选（同星期日衰减点预测约357万，0.8分位约400万）。储能已跨时段对冲预测误差，购电侧再加保守裕度属于重复对冲。正式预测与保守程度仍须按顺序选择方式在全年范围复核，不因本发现直接冻结。

## D012：第二问正式预测方法与保守程度冻结（2026-09-12，按过去数据选择、未来数据评价的裁决）

- **问题：** 方法地图 §3.3 与交接说明第三步要求：不得先看全年结果再挑最优方法；主策略必须按实际总费用、紧急购电量和可行性共同选择。
- **候选：** 预测方法 {七天前同刻 D-7, 同星期日四周衰减平均} × 保守等级 {point, 0.7, 0.8, 0.9}；终端口径 rolling-7d（D011）。
- **证据：**
  - 全年 334 天独立核算回放（rolling-7d、点预测）：D-7 总费用 16,886,077.67 元；同星期日衰减平均 17,295,362.55 元。**D-7 全年低约 409,285 元。** 与 2–4 月短窗口"同星期日更好"结论相反：D-7 保留完整周内规律，长程更稳。（此二值为 D015 因果修复后的同口径结果；修复前旧值分别为 16,884,897.80 与 17,296,322.75 元，排序与结论一致。）
  - 保守程度（2–4 月窗口、rolling-3d）：point 总费用最低（同星期日/point 约357万），0.7 约390万、0.8 约400万、0.9 约415万。**分位保守反而更贵**，因为储能已跨时段对冲预测误差，购电侧再加裕度是重复对冲、只增正常电费。
  - 顺序选择（每30天一个决策时点，只用该时点前已完成日期比较累计实际总费用）：两方法互有胜负（11段中 D-7 被选4段、同星期日被选7段），差距小而不稳定。
- **决定：** 正式采用 **七天前同刻（D-7）+ 点预测（point）+ 滚动7天视野（rolling-7d）** 作为第二问主口径。
- **理由：** ①全年实际总费用最低；②完全因果、实现最简单、可审计；③顺序选择显示两方法差距小，无需为边际收益引入更复杂的分位机制。分位保守的失效给论文提供了"预测更保守≠策略更好"的定量论据。
- **影响：** 以 D-7/point/rolling-7d 运行正式全年回放并生成 result2。分位候选与传统残留作敏感性对照，不进入主结果。

## D013：第三问附件3点预测的10分钟展开（2026-09-12，N3重裁）

- **问题：** 附件3的第h列已确认是 `issue_time+h` 的点预测，必须在不左移、不读取后发批次的前提下展开到10分钟槽。
- **候选：** 相邻整点线性插值、前值保持。只用1月实际光伏选择口径，2月至12月保留为正式评价期。
- **证据：** 1月4429个有历史锚点的目标槽中，线性插值 MAE=61.93 kW、RMSE=133.09 kW；前值保持 MAE=242.04 kW、RMSE=463.22 kW。测试 `src/q3_forecast_mapping_tests.py` 12/12通过。
- **决定：** 主口径采用相邻点预测线性插值。发布时刻的左端点只能取此前已发布批次对该时刻的预测；当前批次提供 `issue+1h` 及以后点。1月1日0:00无历史锚点的首小时使用附件1先验。所有来源满足 `source_issue_time<=decision_time`。
- **性能决定：** 附件3全部35040条点预测一次读取并缓存，禁止在每次0/6/12/18重优化时重新打开工作簿。
- **影响：** N3解除，允许先做Q3单日预测驱动滚动 smoke；通过独立审计后才可运行全年。
- **实施状态：** 单日 smoke 7/7通过；另以2025-02-01 18:00批次对次日12:00的目标值确认跨午夜24小时预测未截断，4/4通过。全年四时点顺序回放、正式 `result3.xlsm` 和独立验证已完成：334天总费用15805751.77元，不可行天数0，独立物理、时间、版本、账本和工作簿检查19/19通过。
- **更新时点证据：** 仅0:00、加入6:00、再加入12:00、再加入18:00的总费用依次为16350694.80、16008998.90、15805748.88、15805751.77元。6:00与12:00有明确收益；18:00边际增费2.90元，实质贡献可忽略。正式结果仍按题面保留四个发布时点，论文据此说明不需要继续增加发布时间。

## D014：第四问价格信息集与正式方法（2026-09-13）

- **问题：** 附件4给出全年真实波动电价，但每天0:00或日内更新时间不能提前读取尚未发生槽位的真实价；必须区分优化用价与核算用价。
- **决定：** 使用双价格通道。优化器仅使用决策时刻可得的 `price_forecast[target_slot|issue_time]`；执行器与核算器使用附件4的 `price_actual[target_slot]`。未来真实价只进入不可执行的完美信息下界。
- **预测选择：** 只用2025年1月15日至30日顺序回放比较D-7和同星期日四周衰减加权。两条分支合计实际费用分别为2069387.34元和2065548.54元，正式选择同星期日四周衰减加权；2月至12月未参与选型。
- **实施：** 第二问式复用D-7负荷/光伏与rolling-7d；第三问式复用附件3光伏线性展开、0/6/12/18更新及D007版本账本。七天视野内所有目标槽共享当前真实决策时刻。
- **结果：** 334天实际费用分别为17743324.88元和16606095.44元；第三问式节省1137229.44元（6.4093%），紧急购电量降低32.7207%。独立验证78/78通过。
- **下界：** 固定各分支正式期初、期末SOC的全年完美信息LP费用约12783515元，仅用于量化信息差距，不是可执行结果。
- **产物：** `outputs/q4/`、`research/C_Q4_validation_report.md`。第四问正式结果闭环。

## D015：多日视野统一真实决策时刻（2026-09-13，致命因果错误修复）

- **问题：** 旧第二问、第三问回放在七天视野中把未来目标日误当成新的决策日，可能读取当前尚未发生的真实值。1月1日反例使第一天计划单槽最大变化166.67 kWh，属于致命因果错误。
- **决定：** 一次滚动求解中的全部目标日统一使用当前真实 `decision_time` 截断历史；最远槽的D-7来源尚未观测时逐槽回退D-14、D-21等更早同星期来源，最后才用附件1先验。
- **验证：** 独立反例6/6、历史失败复现与整改后严格参考对拍均通过；第二问预测、第三问映射、单日回放、账本和主探针回归通过。
- **影响：** 第一问不受影响。第四问只基于修复后接口生成。第二问、第三问旧正式产物需做一次受控重算比较；确认影响后同步其结果和论文，不得继续把旧数值当作最新因果结果。
- **受控重算结论（2026-09-13）：** 第二问修复后总费用 16,886,077.669585 元（旧值 16,884,897.798346 元，+1,179.871239 元），紧急购电量 1,199,863.447137 kWh（旧值 1,199,589.733754 kWh），正常合同电费与未使用合同电不变；变化超过数值容差，第二问正式产物 `outputs/q2/` 已替换为修复后结果（`result2.xlsm` SHA-256 `99cb347f...`），生成器内置审计、三天独立验收 31/31、紧急购电区间合并测试 5/5 通过。第三问修复前后仅差约 $-1.9\times10^{-8}$ 元，属浮点末位误差，正式工作簿与绝对结果不替换，仅更新其相对第二问的比较数字（节省 1,080,325.894684 元、6.397731%，紧急购电量减少 390,636.311230 kWh、32.556731%）。第四问从修复后接口生成，不受影响。
- **同星期预测冷启动（D012 补充）：** 修复因果边界后，同星期日四周衰减平均在滚动视野中一月上旬可能尚无任何已观测同星期来源；此时回退附件 1 典型日先验，一旦存在已观测同星期历史即继续使用衰减平均。该规则属 D010 冷启动原则，不是新建模裁决。修复后同星期日全年总费用 17,295,362.55 元，仍高于 D-7，正式方法（D-7/point/rolling-7d）不变。冷启动测试 10/10 通过。
