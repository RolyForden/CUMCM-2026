# 重要决策

## D001：最终选择 C 题

- **问题：** 在本科组、两名成员、约 1.5 天有效比赛时间的约束下，从候选题中确定最终投入方向。
- **决定：** 选择 C 题“微网与外部电网电力调控策略”。正确性优先的可选排序为 C > B > A；D、E 题不进入本科组候选。
- **理由：** C 题不仅数据完整、baseline 快，而且在模型、算法和结果三个层面都能独立验证：约束与能量账本可机械检查，优化结果可交叉求解，策略可在未参与决策的实际数据上顺序回放。A 的移动边界与物理真值缺失使“数值收敛但模型错误”难以排除；B 的模拟器、接口与有限正式测试构成关键路径风险。
- **证据：** `research/topic_selection_A.md`、`research/topic_selection_B.md`、`research/topic_selection_compare.md`。
- **授权：** 人类已明确委托本次同步得出最终选题结论。
- **复核：** 人类补充“建模和结果正确性为最大评分因素”后重新加权，结论仍为 C；B 因官方模拟反馈升为条件性第二，模拟器不可用时 A 才是实际后备。C 的正确性优势来自可审计和可回放，不是复杂模型名称。
- **比例因素：** 各赛道按相同比例授奖时，参赛人数与奖项数同步变化，选题热度不产生机械胜率优势。当前投票显示 C 最热门，只增加方案同质化风险；在缺少各赛道队伍质量分布的情况下，不据此改选 B 或 A。
- **状态：** 有效。项目已进入 `SOLVING`。

## D002：C 题 13 项 P0/P1/P2 口径冻结（2026-09-12，人类逐项拍板）

> 唯一事实基准：此后所有代码、结果、论文只按本条目口径走。13 项对应 `research/C_problem_map.md` 冻结清单与 `research/C_method_map.md` §1.5。每项给出问题、决定、理由、选错的风险。相关代码风险点：全题共用 144 槽位适配器、统一执行器与统一费用核算器。

### P0（8 项，不解决不能开始正式建模）

**P0-1 时间标签映射 → 起点对齐（映射 A）**
- **决定：** 输入 144 点标签 `00:10`…`0:00+1` 是各时段的**起点**：输入点 k 的值代表区间 `[标签_k, 标签_k+10min)` 的功率/电价，与结果模板区间列（`0:10-0:20`…`0:00+1-0:10+1`）逐槽一一对应，不做平移。
- **理由：** 官方模板首行 `0:10-0:20`、末行 `0:00+1-0:10+1` 与输入 144 点完全对齐，无 `0:00-0:10` 区间（`0:00` 是当天第一个时段的起点，题目只用次日 `0:00+1` 表示当天 24:00）。终点对齐需先平移一格且无法解释模板首行。
- **选错的风险：** 若实际是终点对齐，全部四问 144 时段都错位 10 分钟，Q1 锚点 35126.95 元不复现。
- **落实：** 建独立 144 槽位适配器（输入列→槽位→模板列），按槽位对列写入，禁止按时间字符串拼接；槽位映射恒定，四问共用。已按官方包逐表核验：输入 00:10 开列、末行 0:00+1，result1 首行 0:10-0:20 末行 0:00+1-0:10+1，result2/3/4 首列区间 0:10-0:20 且末尾有"全天购电量/全天购电费"两列（写作时用 146 列/147 列结构，不以本地缺表模板为准）。
- **落实（探针 2026-09-12 验证）：** 该映射的墙钟推论——槽 0 = [00:10,00:20)，槽 142 = [23:50,0:00+1)（跨日），槽 143 = [0:00+1,0:10+1)（完全落在次日）；官方输入无 `00:00` 列，当天 [00:00,00:10) 无输入数据、不参与调度（SOC 不变）。适配器已按此实现并与官方输入表头、result1 模板区间逐列对齐（探针 T1）。若人类复核后推翻该推论，需要同步修改槽位适配器、终端 SOC 约束、模板解释及对应测试，不能只改一处标签。

**P0-2 Q1 初始 SOC → 固定 6000 kWh**
- **决定：** Q1 初始 SOC 固定 E_0 = 6000 kWh，终端约束按"144 槽周期首尾相等"执行（E_144 = E_0，即次日 00:10 的 SOC 回到 6000）。
- **理由：** 附录 1 明确 2025-01-01 0:00 电量为 6000 kWh，是题面唯一给定初值；固定后与 Q2–Q4 从 1 月 1 日 6000 起的 SOC 演化完全一致。
- **选错的风险：** 不固定则模型自选有利初始库存（自由最优首尾 SOC=8550、费用 35118.60 元），与给定初值矛盾且 Q1 与 Q2 无法衔接。
- **敏感性（必做）：** E_0 ∈ {1200, 6000, 10800}。
- **探针记录（已由 D003 确认）：** 映射 A 下"24:00 储电量"= E_143（槽 142 末）。若把终端约束读作 E_143 = E_0，Q1 费用为 34881.454457 元，比主口径（E_144 = E_0，35126.948590 元）低 245.494132 元。人类已选择方案A，差异保留为敏感性对照；裁决记录见 D003 与 `research/C_time_boundary_decision.md`。

**P0-3 效率 90% → 充、放电各 90%（母线侧计量）**
- **决定：** η_c = η_d = 0.9。充电入库 = 母线侧充电量 × 0.9；向母线放电 `d` 时库存减少 `d/0.9`。**母线侧能量平衡** `grid_delivered + pv_used + discharge = load + charge`，且 `pv_curtail = pv_available − pv_used`；所有功率/电量计量均在母线侧。
- **理由：** "充放电效率为 90%" 最自然的读法；母线侧计量使能量平衡与结算（电价×母线侧购电）同侧，无隐藏折算。
- **选错的风险：** 若实际是往返 90%，全年费用低约 3.8%（Q1 锚点变 33801.50 元），结论会系统性偏乐观。
- **敏感性（必做）：** 往返口径 η_c = η_d = √0.9，另做总效率 ±5%。

**P0-4 弃光/反送 → 允许弃光、禁止反送**
- **决定：** 光伏富余且电池已满时允许弃光（`pv_curtail ≥ 0`），不允许向外网反送（`grid_delivered ≥ 0`）。弃光不设自由变量，而由 `pv_available − pv_used` 计算。
- **理由：** 题面未给售电规则；不允许弃光则富余且电池满时模型人为不可行。
- **选错的风险：** 禁止弃光会在高光伏日出现不可行；允许反送会引入题面没有的收益项。
- **敏感性（必做）：** 弃光开关（检查全年可行天数）。

**P0-5 Q2 信息集 + Q4 电价可知性 → 严格因果；1 月为 warm-up；Q4 主口径电价不可知**
- **决定：** ① 每天 0:00 计划只用 0:00 及之前已发布的信息（历史实测、当时已发布的预报），**当天实际负荷/PV 未知**；负荷预测只能用历史数据构造。② 1 月为 warm-up 窗口（预热预测器 + 按规则推进 SOC），2–12 月为正式回测输出期。③ Q4 主口径按**当天未来电价在决策时不可知**：用决策时可获得的历史电价预测（如 D-7/同星期日基线），结算仍按附件 4 真实电价；真实电价 oracle 只作不可执行下界对照。
- **理由：** 附件给出全年真实数据不代表决策时已知；否则 Q2 紧急购电被人为压成零、Q3 预测更新失去意义。Q4 电价可知性题面未明说，因果口径是可执行且能展示"可执行性代价"的主口径。
- **选错的风险：** 使用未来真值 = 信息泄漏，Q2 零紧急购电或异常低成本是典型泄漏特征，直接判错。
- **敏感性（必做）：** 1 月纳入调度期的对照；Q4 真实电价 oracle 并列展示。**结果模板只提交一个主口径。**

**P0-6 跨日 SOC 与冷启动 → 连续传递**
- **决定：** 前一天末 SOC 传到下一天（E_0(d+1) = E_144(d)）；1 月 1 日 0:00 从 6000 kWh 起，1 月按同规则演化到 2 月 1 日，2 月 1 日**不重置**。
- **理由：** 每天重置 6000 会虚构能量、破坏物理连续性（方法地图失败模式 #3/#20）。
- **选错的风险：** 逐日复位会系统性低估/扭曲跨日储能价值。
- **敏感性（必做）：** 逐日复位 6000 作为失败模式对照。

**P0-7 电池实时重调 → 按计划执行，不做实时重调**
- **决定：** 实际负荷/PV 揭示后，电池**严格按 0:00（或最近一次更新）计划执行**，不因实际值偏离而临时改充放电；缺口全部走紧急购电（5 倍）。每天 0:00 的 LP 自带安全裕度（裕度策略用 0.8 分位等候选在 walk-forward 上竞标，见 P0 之外的 Q2 方法选择）。
- **理由：** 与题面"除紧急购电费用外，其他时间段按计划购电量计费"的字面读法一致；费用账本最简（计划 + 紧急），Q2/Q3 执行器统一。
- **选错的风险：** 若允许实时重调，缺口与费用结构都会不同；本决定下"电池有电仍买紧急电"是正确行为而非错误（论文必须解释）。
- **敏感性（必做）：** 允许实时重调的另一规则作为对照口径，不入主结果。

**P0-8 Q3 调整结算 → 替代价式，相对上一版**
- **决定：** 调整结算为**替代价**（重叠部分不重复计费）：每时段 `C_t = p_t·min(q_old, q_new) + 0.5p_t·(q_old − q_new)_+ + 1.5p_t·(q_new − q_old)_+`。**调整基准为上一版计划**：q⁶ 相对 q⁰、q¹² 相对 q⁶、q¹⁸ 相对 q¹²。
- **理由：** "计划高于调整的部分按 50% 违约电价"的最自然读法是被替换部分按 50% 结算，不是额外罚金；相对上一版与滚动重优化语义一致。下调后实际总费用可以低于原计划费用。
- **选错的风险：** 额外罚金/相对初始口径费用会显著更高（本地资料中 0.13%–3.18% 的差异正是口径不同造成，非算法优劣）；重复计费是硬错误。
- **敏感性（必做）：** 相对初始计划基准的对照，仅作敏感性。

### P1（3 项，建模过程中必须冻结）

**P1-1 "每天电价相同"声明 → 日内曲线每天重复**
- **决定：** 指附件 1 的 144 点日内电价曲线（0.371–1.395 元/kWh）每天重复，不是一天内恒价。论文必须显式声明此读法。
- **理由：** 附件 1 实测 144 点电价明显波动；"每天相同"只能指曲线逐日重复。
- **选错的风险：** 日内恒价会抹掉日内套利结构，Q1 储能调度失去意义。

**P1-2 预报映射与视野 → 零阶保持主口径**
- **决定：** 附件 3 的"预报 k 小时"= 发布时刻后第 k 个小时的整点光伏预报（已核验：首日 0:00 发布的首小时预报为 0，凌晨无光照属正常）；整点 → 10 分钟粒度用**零阶保持**（预报 k 小时值作用于发布后第 k 个小时内全部 6 个 10 分钟时段），分段线性/PCHIP 作敏感性，以因果运行费用选优（禁止用未来实际光伏作插值锚点）。跨日 24h 视野**不截断**：跨日有效时段保留，只用于重优化未执行时段。附件 3 日期列为合并单元格（365 天 × 4 发布时点），预处理必须前向填充。
- **理由：** "整点预报"语义下零阶保持最忠实；插值方案由费用证据决定，不凭视觉平滑。
- **选错的风险：** 错位/截断/拼接后发预测会直接改变 Q3 可用信息（失败模式 #15/#16）。
- **敏感性（必做）：** 分段线性、PCHIP 对照。

**P1-3 "是否需要其他时刻预报"判据 → 费用+紧急+稳定性**
- **决定：** 用扣除调整成本后的总费用、紧急购电量和策略稳定性（调整幅度/次数、SOC 轨迹稳健性）共同判断，不以 PV 预测误差（RMSE/MAE）单独决定；论文以更新时点逐级消融（0:00 only → +6 → +12 → +18）定量回答。
- **理由：** 预测更准不等于策略更好；业务价值必须用统一核算器下的实际费用证明。
- **选错的风险：** 只报预测误差无法回答"是否需要引入其他时刻的预报"这一题面明确要求。

### P2（1 项，输出阶段确认）

**P2-1 结果模板完整性 → 展开全部 334 天 + 补齐缺表**
- **决定：** result2/result3/result4-2/result4-3 的省略号行展开全部 334 天（2025-02-01 至 12-31），保持原工作表与列结构；写入按槽位对列，不按标签字符串拼接。**模板缺失的工作表按题面补齐**（见下方"数据源核实"），官方模板原有的隐藏表（Sheet1/Sheet2 宏信息）保持原样。result2/3/4 表格尾列含"全天购电量/全天购电费"两列，必须填写。
- **理由：** 题面要求保存完整策略；缺表不填 = 题面要求未完成。
- **选错的风险：** 只填模板已有行会被判不完整。

### 数据源核实（2026-09-12 官方包逐表核验，属 D002 一并冻结）

- **官方包本机副本缺表：** `CUMCM2026Problems/C题/附件/附件2.xlsm` 只有"小区负载"表，缺题面要求的"光伏发电实际功率"表；附件 5 的 result1/result2/result4-2/result4-3 缺"充放电量"与"紧急购电"工作表（附件 3 与 result3 完整）。
- **决定：** 用 `reference/chapt1/26国赛C题成品论文3/2026Cnew/data/附件2.xlsx` 的光伏表（52560 值，min 0、max 10216.2 kW，无缺失无负值）作为替补数据源，与官方包负载表逐值一致（53070 值 mismatch=0）；附件 1/3/4 官方包与副本逐值一致。正式提交结果前如能重新从官网下载原包，比对 SHA-256 后替换；模板补齐时以题面（`tmp/pdfs/c-problem-review/C题.txt`）为准，保持官方模板原有表结构与格式。
- **数据固定口径：** Δt = 1/6 h，功率 × Δt 转 kWh；充/放电量上限 5000/6 = 833.333... kWh/格；SOC ∈ [1200, 10800]；电价/功率计量在母线侧。求解器为配置项（HiGHS 双引擎对拍），不入冻结清单。

### 不属本条目、但必须遵守的既有约束

- Q1 数值锚点（条件性回归值）：本口径下 Q1 应为 **35126.948589 元**、能量平衡最大残差 ~1e-13（已在冻结口径下独立复算），探针通过标准之一。
- 优化器目标值仅供对照，论文数字一律以执行器+核算器复算为准；Q1–Q4 共用同一核算器。

## D002 补充：探针整改后的实施状态与待裁决项（2026-09-12）

> `2250523` 的 41/41 仅保留为整改前基线。交叉验证发现结构缺陷后，代码在提交 `7464ccca35ad5a0eb454d0441fb6e051264e1582` 完成整改：主探针 48/48、独立交叉验证 39/39。证据见 `research/C_probe_cross_validation.md`。以下只同步实施事实，不改变上方人类冻结口径；N4已由D003裁决，N1–N3继续等待人类裁决。

### 已落实的结构性修正（TASK_C_probe_model_corrections.md）

1. **弃光只能来自光伏**：内核以 `pv_used ≤ pv_available` 表达，`pv_curtail = pv_available − pv_used` 由恒等式推导；不再存在自由 `w`。T4的准确结论是：弃光出口已关闭，连续LP的电池循环假出口由审计捕获，独立互斥MILP确认无合法出口时不可行。
2. **Q1 购电语义**：`grid_contract = grid_delivered`，`grid_unused = 0`，`grid_emergency = 0`；目标只算 `Σ price·grid_contract`，Q2 规则不混入。
3. **计划版本不可变**：`src/core/executor.py` 的 `PlanRecord` 按 target_slot/issue_time/plan_version/previous_version/quantity/increase/decrease 追加新版本，不覆盖旧版本；Q3 多次结算公式未冻结。
4. **因果数据接口**：已拆分 `build_q1_inputs`、`history_actuals_as_of`、`replay_actual_day`、`forecast_vintage_as_of`。历史实际值按 `observed_at ≤ decision_time` 截断，预报按 `issue_time ≤ decision_time` 截断；附件发布日期不再冒充观测时刻。
5. **按日期读取**：全年宽表按标准化日期唯一定位，缺失、重复、非144个数值均报错，不再固定读取第2行。
6. **独立核算**：空记录、SOC断链、重复/跳号/逆序槽位、同时充放电和Q1购电语义错误均被拒绝；核算器不调用LP状态更新函数。
7. **可行基线**：探针已用“无储能基线”替换末槽直接补费用的旧规则法，基线经过同一执行器和核算器，且费用不低于LP。
8. **官方模板回写**：复制官方 `result1.xlsm` 到系统临时目录，以 `keep_vba=True` 只写目标单元格并回读；已验证144槽、工作表顺序/隐藏状态、非目标单元格和VBA容器保持。

### 待裁决项及阻塞范围

- **N1 Q2 多买电去向**：0:00 计划含安全裕度，实际负荷低于计划时多余购电如何处理（按计划交付照常计费 vs 其他余电出口）。候选关系 `grid_unused = grid_contract − grid_delivered` 仅作接口预留。
- **N2 Q3 多次调整累计结算**：单次替代价式不能直接对多次更新逐项求和。逐次现金流候选 `C = p·q⁰ + Σ_k(−0.5p·(q^{k−1}−q^k)_+ + 1.5p·(q^k−q^{k−1})_+)`（电价 1 时 100→80→100 累计 120）只进单元测试/待裁决说明。
- **N3 预测首小时映射与冷启动**：附件 3"预报 1 小时"对应发布后第 1 个小时的哪个 10 分钟槽位；Q2 预热窗口与 2 月 1 日起始 SOC 的衔接规则。
- **N4（已裁决，见 D003）**：采用映射A和 `E_144=E_0`，不再阻塞Q1；`E_143=E_0`只作敏感性对照。

在 N1–N3 未确认前，不启动 Q2–Q4 全年正式求解。

## D003：N4 时间边界选择方案A（2026-09-12，人类确认）

- **问题：** 附件与result1模板的144槽覆盖当天00:10至次日00:10，而题面“0:00与24:00储电量相同”的墙钟字面对应次日00:00；必须确定终端SOC落在 `E_143` 还是 `E_144`。
- **决定：** 选择方案A。保持起点对齐：slot 0=`[当天00:10,00:20)`，slot 142=`[当天23:50,次日00:00)`，slot 143=`[次日00:00,00:10)`；当天`[00:00,00:10)`无输入且假定无动作。完整执行附件144槽后约束 `E_144=E_0=6000 kWh`。
- **主结果：** Q1费用采用 `35126.948590` 元；该值现为已冻结方案A下的正式主口径结果。
- **对照：** `E_143=E_0` 的 `34881.454457` 元只作时间边界敏感性，不写入正式结果模板。
- **理由：** 方案A完整覆盖官方输入和模板的144槽，并避免最后一槽在没有终端库存价值时无约束消耗电池；Q2–Q4继续按同一144槽周期传递 `E_144(d)=E_0(d+1)`。
- **影响：** N4解除；允许生成Q1正式结果并撰写其墙钟解释。N1–N3仍未裁决，Q2–Q4全年正式求解继续禁止。
