# 第三问预测展开阶段报告

附件3的35040条记录一次加载并缓存，每条保持 `valid_time=issue_time+lead_hour`，不把首小时预测左移。每次0/6/12/18发布后，当前时刻锚点取此前已发布批次，后续整点取当前批次，并在相邻点间线性插值。

点到10分钟的选择只使用1月数据。4429个有历史锚点的槽中，线性插值 MAE 为61.93 kW、RMSE为133.09 kW；前值保持分别为242.04和463.22 kW。因此D013冻结线性插值。1月1日0:00没有前一批次时，首小时使用附件1因果先验。

`src/q3_forecast_mapping_tests.py` 共12项全部通过，覆盖记录完整性、有效时刻、缓存命中、整点间插值、四批次144槽覆盖、后发批次拒绝和1月方法选择。机器结果为 `experiments/q3_forecast_mapping_results.json`。

滚动视野不会在午夜截断当前批次仍有效的24小时预测。实现层把完整7天视野的目标时刻交给同一展开器：当前批次覆盖范围内使用附件3，超出24小时后才回退D-7因果基线。独立回归选取2025-02-01 18:00批次对次日12:00的预测，优化器收到的数值与该批次线性展开值均为7444.3125 kW，4/4检查通过；机器结果见 `experiments/q3_crossday_forecast_test.json`。
