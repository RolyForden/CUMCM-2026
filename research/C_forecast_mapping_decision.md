# 附件3预测映射复审说明（N3）

## 当前状态

D006 是复审前裁决稿，其关键结论已由 D007 撤回。D013 已完成 N3 重裁。附件3原始值按点预测保存：

```text
valid_time = issue_time + lead_hour
```

主口径采用相邻整点线性插值；发布时刻左端锚点只取此前已发布批次，当前批次从 `issue_time+1h` 开始。1月1日0:00缺失的首小时锚点使用附件1先验。

## 撤回内容

撤回 D006 中以下结论：

```text
预报1小时 -> [issue_time, issue_time + 1h)
预报h小时 -> [issue_time + (h-1)h, issue_time + h h)
N3 解除
允许实现附件3预测展开适配器
```

## 复审证据

附件3更支持点预测语义，而非前一小时区间均值：

| 映射方式 | 全年 MAE | lead=1 MAE |
|---|---:|---:|
| `issue_time + h` 点对齐 | 186.82 kW | 18.69 kW |
| D006 前一小时区间均值 | 446.53 kW | 307.01 kW |

当前 `src/core/data_io.py` 已把附件3原始记录写成 `valid_time = issue_time + lead_hour`，与点预测解释一致。

## D013 重裁证据

- 选择窗口：2025年1月，2月至12月不参与映射方法选择。
- 有历史锚点的目标槽：4429。
- 线性插值：MAE 61.93 kW，RMSE 133.09 kW。
- 前值保持：MAE 242.04 kW，RMSE 463.22 kW。
- 方案A末槽起点为次日0:00，正好使用0:00批次的h24点或18:00批次的h6点，不需读取下一日0:00新批次。
- 附件3一次读取缓存；测试 `src/q3_forecast_mapping_tests.py` 12/12通过，结果见 `experiments/q3_forecast_mapping_results.json`。

## 当前允许范围

N3已解除。允许实现Q3单日预测驱动滚动 smoke；只有在预测覆盖、版本锁定、物理执行和独立账本均通过后，才允许运行Q3全年。
