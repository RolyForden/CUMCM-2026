# 附件3预测映射复审说明（N3）

## 当前状态

D006 是复审前裁决稿，其关键结论已由 D007 撤回。附件3原始值先按点预测保存：

```text
valid_time = issue_time + lead_hour
```

点预测如何展开到 10 分钟槽位尚未裁决。N3 继续阻塞正式 Q3 预测适配器和预测驱动滚动窗口。

## 撤回内容

撤回 D006 中以下结论：

```text
预报1小时 -> [issue_time, issue_time + 1h)
预报h小时 -> [issue_time + (h-1)h, issue_time + h h)
N3 解除
允许实现附件3预测展开适配器
```

## 复审证据

附件3更支持点预测语义，而非前一小时区间均值：

| 映射方式 | 全年 MAE | lead=1 MAE |
|---|---:|---:|
| `issue_time + h` 点对齐 | 186.82 kW | 18.69 kW |
| D006 前一小时区间均值 | 446.53 kW | 307.01 kW |

当前 `src/core/data_io.py` 已把附件3原始记录写成 `valid_time = issue_time + lead_hour`，与点预测解释一致。

## 待重裁问题

- 点预测如何展开到 10 分钟槽位：向前保持、向后保持、线性插值或其他方法。
- 发布后首小时如何补齐，不能把 `f_1` 左移到发布后立即一小时。
- 方案A末槽 `[次日00:00, 次日00:10)` 的预测来源。
- Q2 光伏预测冷启动口径。
- Q2/Q3 日级终端处理：每日回初值只能作为探针计划层临时约束，不能直接成为全年正式主模型。

## 当前允许范围

允许只读验证：

- 任意预测记录满足 `valid_time = issue_time + lead_hour`。
- 预测记录不使用未来发布信息：`issue_time <= decision_time`。
- 检查方案A末槽和发布后首小时是否存在信息缺口。

禁止：

- 实现正式10分钟预测展开器。
- 运行 Q3 预测驱动滚动窗口。
- 运行 Q2-Q4 全年正式模型。
