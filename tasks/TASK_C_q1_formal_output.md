# TASK：按方案A生成Q1正式结果

- 状态：已完成（2026-09-12）。
- 目标：在D002参数与D003方案A下生成官方 `result1.xlsm` 副本，并完成求解、执行、独立核算、模板回读和结构保持检查。
- 输入：`data/raw/official/附件1.xlsm`、`data/raw/official/附件5/result1.xlsm`。
- 输出：`outputs/q1_scheme_a/result1.xlsm`、完整调度明细、审计证据和实验记录。

## 约束

- 不修改 `data/raw/`；只填写官方模板“计划购电量”表 B2:B145。
- 固定 `E_0=E_144=6000 kWh`，slot 0 从当天 00:10 开始，执行完整144槽。
- Q1中 `grid_contract=grid_delivered`，未用合同电和紧急购电均为0。
- 正式费用以执行器和独立核算器复算值为准；双求解器只用于交叉核对。
- 不启动Q2–Q4，不处理N1–N3。

## 验收

- HiGHS dual simplex 与 IPM 均可行且费用差小于 `1e-6`。
- 独立核算器通过，费用与优化结果差小于 `1e-6`，最大能量残差小于 `1e-9`。
- `E_144=6000`，无同时充放电，SOC与功率均不越界。
- 回读144个购电量与求解结果逐槽一致，首尾标签分别为 `0:10-0:20` 和 `0:00+1-0:10+1`。
- 官方模板的工作表名称、顺序、隐藏状态及非目标单元格保持不变；原模板哈希在任务前后不变。
- 输出经过可视化抽查，原探针与独立交叉验证仍全部通过。

