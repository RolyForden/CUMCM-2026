# TASK：Q1 结果验证与论文定稿

- 状态：已完成（2026-09-12）。
- 目标：不重做 Q1 主模型，独立核对正式 CSV、用显式 SOC 的互斥 MILP 对拍，并完成参数敏感性、输入扰动稳健性和 Q1 正文。
- 主输入：`data/raw/official/附件1.xlsm`、`outputs/q1_scheme_a/q1_dispatch.csv`、`q1_audit.json`、`q1_paper_tables.json`。
- 产物：可重复运行的验证脚本、机器可读验证结果、必要图表、`research/C_q1_validation.md`、写入 `paper/数模通用模板.tex` 的 Q1 正文。

## 验证路线

1. 独立读取原始附件与最终 CSV，逐槽复算单位换算、能量平衡、SOC、费用及导出一致性；覆盖典型、极端、边界和固定种子随机槽。
2. 不调用主 LP 内核，建立显式 SOC、充放电互斥的 MILP，对比最优费用、总购电量、逐槽差异和约束。
3. 扰动初始 SOC、效率、容量与功率，量化费用和相对无储能收益变化。
4. 对负荷/PV 加入有界、时间相关的小幅扰动，重新求解并统计费用与核心结论稳定性。
5. 先形成验证报告，再以实际输出数字撰写 Q1 正文；不编造未运行结果。

## 闸门

- 若发现数据、单位、索引、边界或 CSV 硬错误，先记录、修复并重跑；否则不修改主模型和正式 CSV。
- Q1 不含拟合或参数估计，不做 K-fold；原因写入报告和论文。
- D003 方案 A 为人类冻结口径，只作为已知模型风险披露，不擅自改判。
