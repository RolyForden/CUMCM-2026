# CUMCM 轻量多 Agent 协作系统

## 0. 当前目标

为两名队员建立一套适用于约 1.5 天数学建模竞赛的：

- Git 协作系统
- 多 Agent 协作系统
- 上下文压缩与会话接力机制
- 任务隔离机制
- 最小质量审查机制

本系统优先级：

> 解题效率 > 工作流完整性

禁止为了形式化而增加没有明显收益的流程。

当前阶段：

```text
BOOTSTRAP
```

在收到：

```text
ENTER TOPIC_SELECTION
```

之前，不得开始正式选题或分析真实竞赛题。

---

# 1. 核心原则

## 1.1 Git 是长期记忆

Git 负责保存：

- 历史版本
- 失败实验
- 代码修改
- 文档变化
- 重要决策历史

不要试图把完整历史一直塞在 Agent 上下文里。

---

## 1.2 main 代表当前可信版本

`main` 只保存：

> 当前认为可靠、可以继续向下工作的版本。

实验、尝试、验证应优先在：

```text
branch
或
worktree
```

中进行。

重大结果未经检查，不直接进入 `main`。

---

## 1.3 Agent 负责执行，人类负责拍板

Agent 可以：

- 检索
- 推理
- 建模
- 写代码
- 跑实验
- 审查
- 写作

人类负责：

- 最终选题
- 重大题意解释
- 最终模型选择
- 重大假设
- 模型冻结
- 论文冻结

---

# 2. 两名队员角色

## 人类 A：系统整合

重点负责：

- Git
- Agent 路由
- 任务拆解
- 项目状态
- 结果整合
- 论文与实验一致性
- 时间控制

关注问题：

> 整个系统有没有跑偏？

---

## 人类 B：模型挑战

重点负责：

- 数学建模
- 替代方案
- 模型合理性
- 实验设计
- 结果复现
- 质疑当前方案

关注问题：

> 这个模型真的站得住吗？

---

两人不是上下级关系。

重大分歧优先通过：

```text
独立分析
实验
证据
复现
```

解决。

---

# 3. Agent 类型

不建立大量固定 Agent。

只保留以下逻辑角色，根据任务临时调用。

## 总控 Agent

负责：

- 看当前状态
- 拆任务
- 路由任务
- 判断下一步

总控不要亲自承担大量具体研究工作。

---

## 检索 Agent

负责：

- 查论文
- 查方法
- 查数据集
- 查领域资料

---

## 分析 Agent

负责：

- 理解题目
- 数学推导
- 模型设计
- 指标与约束设计

---

## 执行 Agent

负责：

- 写代码
- 数据处理
- 跑实验
- 画图
- 输出结果

---

## 审查 Agent

原则：

> 假设当前结果可能是错的。

重点找：

- 数据泄漏
- 过拟合
- 单位错误
- 公式与代码不一致
- 错误指标
- 不合理假设
- 不可复现
- 结论超过证据

---

## 写作 Agent

只允许使用已经验证过的：

- 模型
- 实验
- 图表
- 结论

不得为了论文叙事自行制造结果。

---

# 4. 最小仓库结构

创建：

```text
CUMCM/
│
├── AGENTS.md
├── STATE.md
├── SESSION_HANDOFF.md
├── DECISIONS.md
├── README.md
├── .gitignore
│
├── tasks/
│
├── data/
│   ├── raw/
│   └── processed/
│
├── src/
│
├── experiments/
│
├── figures/
│
├── research/
│
└── paper/
```

除非后续确实需要，不要提前创建更多管理文件。

---

# 5. AGENTS.md

`AGENTS.md` 必须保持短小。

建议不超过约 100 行。

它只负责记录不会频繁变化的规则，例如：

```text
1. 工作前先读 STATE.md 和当前 TASK。
2. 不直接破坏 main。
3. data/raw 不允许修改。
4. 重要实验必须可复现。
5. 不确定的信息不得伪造。
6. 重大决策由人类确认。
7. Agent 不默认读取整个仓库。
8. 任务完成后必须更新交接信息。
9. 不允许因为已有结论而跳过必要验证。
10. 不允许为了形式化增加无意义流程。
```

`AGENTS.md` 是宪法，不是项目百科。

---

# 6. STATE.md

`STATE.md` 是整个项目最重要的上下文文件之一。

它只记录：

> 项目现在是什么状态。

禁止把它写成完整历史。

建议保持在 1～2 屏以内。

格式：

```markdown
# 当前状态

阶段：

当前题目：

## 当前主路线
- Q1：
- Q2：
- Q3：

## 已确定
-

## 已否决
-

## 当前主要风险
-

## 当前最高优先级
1.
2.
3.
```

任何时候都优先保证 `STATE.md` 简洁、准确、最新。

---

# 7. TASK：任务级上下文

复杂工作不要直接通过长对话分配。

为任务创建一个短文件：

```text
tasks/TASK_xxx.md
```

格式：

```markdown
# TASK

## 目标

## 输入

## 必读
-

## 要做
-

## 不要做
-

## 验收标准
-

## 输出位置
-
```

只写完成当前任务真正需要的信息。

---

# 8. 会话上下文原则

## 推荐：一任务一会话

一个相对独立的任务，尽量对应一个独立 Agent 会话。

例如：

```text
Q1 EDA
→ 会话 A

Q1 Baseline
→ 会话 B

Q1 模型审查
→ 会话 C
```

不要让一个 Agent 从比赛开始一直聊到比赛结束。

---

# 9. SESSION_HANDOFF.md

当：

- 当前任务完成
- 会话过长
- 出现上下文压缩
- 经历大量 debug
- 工作类型明显变化
- Agent 开始遗忘之前结论

应主动结束当前会话。

结束前更新：

```text
SESSION_HANDOFF.md
```

固定格式：

```markdown
# 会话交接

## 已完成
-

## 当前结论
-

## 关键证据 / 文件
-

## 尚未解决
-

## 下一步
-

## 不要重复
-
```

要求：

> 只写下一名 Agent 为继续工作必须知道的信息。

禁止写成长篇聊天摘要。

---

# 10. 新会话启动顺序

任何新 Agent 会话优先按照：

```text
1. AGENTS.md
2. STATE.md
3. 当前 TASK
4. SESSION_HANDOFF.md
5. 与 TASK 直接相关的代码 / 数据 / 实验
```

读取。

不要默认：

```text
读取整个仓库
读取全部历史
读取所有研究资料
加载所有 Skills
```

---

# 11. 上下文冷热分层

所有信息分为三层。

## 热上下文

当前任务必须知道：

- STATE
- TASK
- 当前代码
- 当前数据
- 当前结果

直接加载。

---

## 温上下文

可能需要：

- 旧实验
- 某个关键决策
- 某篇论文
- 某个失败方案

需要时再读。

---

## 冷上下文

例如：

- Git 历史
- 已结束任务
- Debug 过程
- 被否决旧方案
- 大量旧论文
- 老会话内容

默认不加载。

需要时通过 Git 或文件检索恢复。

---

# 12. Skills 加载原则

Skills 必须按需加载。

例如当前任务是：

```text
查找优化模型相关论文
```

只需要加载：

```text
文献检索 Skill
联网研究 Skill
```

不要同时加载：

```text
论文写作
绘图
神经网络
LaTeX
其他无关 Skills
```

原则：

> TASK 决定 Skill，而不是 Agent 默认携带全部 Skill。

---

# 13. 上下文切换规则

不要被动等到上下文彻底压缩后才切会话。

满足以下任意条件时，可以主动生成 handoff 并开新会话：

### 条件 A
一个任务已经完成。

### 条件 B
从分析转入另一种明显不同的工作。

例如：

```text
文献分析
→
大量代码实现
```

### 条件 C
经历了大量报错与 Debug。

### 条件 D
Agent 开始重新讨论已经否决的问题。

### 条件 E
Agent 忘记已确定的重要结论。

### 条件 F
当前上下文大量内容已经不再服务下一步任务。

---

# 14. DECISIONS.md

只记录真正重要、后续可能反复涉及的决策。

不要记录琐碎决定。

格式：

```markdown
## D001

问题：

决定：

原因：

证据：

状态：
有效 / 已推翻
```

例如：

```text
D003

问题：
Q2 是否继续使用 LSTM？

决定：
否。

原因：
样本量不足，当前验证结果不稳定。

证据：
Q2-E07、Q2-E08。
```

Agent 遇到重要路线问题时先检查 `DECISIONS.md`，避免重复研究。

---

# 15. 实验记录

不要设计复杂实验管理系统。

每个重要实验至少记录：

```text
实验 ID
任务
方法
核心参数
随机种子
指标
代码位置
输出
结论
commit
```

例如：

```markdown
# Q1-E04

方法：Random Forest

seed：42

结果：
RMSE = 0.182

Baseline：
RMSE = 0.211

结论：
目前优于 baseline。

风险：
尚未测试 seed 稳定性。

代码：
src/q1/rf.py
```

---

# 16. 最小审查机制

不是所有工作都需要审查 Agent。

以下情况必须审：

- 题意理解
- 核心数据处理
- 主要模型
- 关键数学公式
- 关键实验结果
- 最终图表
- 最终论文结论

简单工作可以跳过正式审查。

---

# 17. Review 输出格式

保持很短：

```markdown
# REVIEW

结论：
PASS / FAIL / 有风险

主要问题：
1.
2.

证据：

必须修改：

建议：
```

不要生成形式化但没有实际内容的长审查报告。

---

# 18. Git 协作

推荐：

```text
main
```

作为稳定版本。

具体任务：

```text
task/q1-eda
task/q1-baseline
task/q2-model
review/q1
```

可以使用 branch 或 worktree。

---

# 19. Git 工作原则

Agent 可以：

```text
读取 main
创建 branch
commit
测试
提交候选结果
```

重大修改进入 main 前：

```text
工作结果
↓
检查
↓
必要时 Review
↓
人类确认
↓
merge
```

不要建立复杂审批层级。

---

# 20. 数据原则

```text
data/raw/
```

不可修改。

处理后的数据进入：

```text
data/processed/
```

重要数据处理必须尽量能通过代码复现。

---

# 21. Baseline First

任何主要问题先得到简单 baseline。

不要一开始就进行复杂 Agent 搜索和复杂模型堆叠。

顺序推荐：

```text
理解题目
↓
EDA
↓
简单 Baseline
↓
验证流程正确
↓
不同模型方向并行探索
↓
选出候选
↓
深入优化
```

---

# 22. Agent 并行原则

并行应该用于：

> 不同假设之间的竞争。

例如：

```text
Agent A：经典统计模型
Agent B：树模型
Agent C：数学优化模型
```

而不是：

```text
Agent A：XGBoost depth=5
Agent B：XGBoost depth=6
Agent C：XGBoost depth=7
```

模型方向确定以后，再进行参数细化。

---

# 23. 独立验证原则

不要把：

> 三个 Agent 阅读同一个结论后都表示赞同

视为真正交叉验证。

更有价值的是：

```text
A 方独立建模
↓
得到结论

B 方不读完整推理
↓
独立复现 / 攻击
↓
比较
```

---

# 24. 总控 Agent 不应知道所有细节

总控只需要主要读取：

```text
STATE.md
当前 tasks
关键实验结果
必要的 DECISIONS
```

总控无需知道：

- 每个 Python Bug
- 每次安装失败
- 每次参数尝试
- 所有论文全文

保持总控上下文干净。

---

# 25. Writer 也不要读取全部历史

写作 Agent 优先读取：

```text
STATE.md
最终模型代码
关键实验
关键图表
DECISIONS.md
已冻结的数学表达
```

不要把完整研究历史全部塞给 Writer。

---

# 26. 项目阶段

只保留必要阶段：

```text
BOOTSTRAP
↓
TOPIC_SELECTION
↓
SOLVING
↓
MODEL_FREEZE
↓
PAPER
↓
FINAL_CHECK
```

不增加更多状态，除非实际发现有必要。

---

# 27. BOOTSTRAP 当前部署内容

现在只执行以下内容：

```text
1. 初始化 Git 仓库。
2. 创建最小目录结构。
3. 创建 AGENTS.md。
4. 创建 STATE.md。
5. 创建 SESSION_HANDOFF.md。
6. 创建 DECISIONS.md。
7. 创建 tasks/。
8. 创建 data/raw 与 data/processed。
9. 创建 src/、experiments/、figures/、research/、paper/。
10. 配置 .gitignore。
11. 确认 Git branch / worktree 可用。
12. 确认 Python 环境。
13. 检查可用 API / Skills / 外部工具。
14. 不自动加载所有 Skills。
15. 进行一次极小 Dry Run。
16. commit 当前环境。
```

---

# 28. Dry Run

只测试工作流，不测试复杂模型。

例如：

```text
创建一个极小人工数据
↓
建立 TASK
↓
创建 branch
↓
运行简单回归
↓
保存实验结果
↓
生成简短 Review
↓
更新 SESSION_HANDOFF
↓
模拟 merge
```

不要使用真实竞赛题。

---

# 29. BOOTSTRAP 完成输出

环境部署完成后，只需要输出：

```text
BOOTSTRAP 状态：通过 / 未通过

Git：
Python：
branch/worktree：
Skills：
API：
Dry Run：

已知问题：

建议：

READY FOR HUMAN COMMAND: ENTER TOPIC_SELECTION
```

然后停止。

---

# 30. 最高原则

整个比赛期间持续执行：

> 不为工作流而工作。

如果某个文件、流程、Agent 或审查步骤没有明显帮助：

> 可以删除。

如果一个上下文文件越来越长：

> 压缩成当前状态，而不是继续追加。

如果一个 Agent 已经完成当前任务：

> 尽早结束会话。

如果需要历史：

> 去 Git / 文件中按需读取，不要长期背在上下文里。

如果 Skill 当前任务不用：

> 不加载。

如果可以通过一个简单实验解决争议：

> 不进行长时间 Agent 辩论。

最终目标只有一个：

> 在有限时间和有限上下文下，持续保持正确方向、高质量证据和高效并行。