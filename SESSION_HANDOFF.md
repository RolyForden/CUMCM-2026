# 会话交接

## 终稿非附录保守整改（2026-09-13）

- 按人类最新指令，附录保持基线版本，不补指定日期表，不生成完整代码附录，不重打支撑材料包。
- 论文删除无来源外部背书，修正Q2执行映射、评价窗口、Q1时间标签和Q4收益归因，AI用途范围已补齐；实际模型版本及人工复核真实性仍由两位队员确认。
- 未改冻结模型、正式结果或全年回放。第11条指定日期明细和完整代码附录问题按指令保留，不误报为已修复。

## 终稿22项自查（2a8f41e）

按人类要求完成 `research/AI_self_check_table.md` 原22项检查，报告 `research/C_final_AI_self_check.md`。当前PDF47页；合格6、存在问题10、待核实6（条目重叠，不是错误数量或评分）。新版Q4-2 8月9日24:00反例回读已修，不能照搬旧问题。仍需修Q2受电量等式、MAE表评价窗口、指定日期购电/紧急区间明细、Q1墙钟标签与完整代码附录。AI实际版本与人工参与据真实过程确认，不据文风判断。本轮未改论文、模型、正式结果或原自查表，未重跑全年。

## 2026-09-13 论文审查交接

- 基线 `正式建模` / `89f447c`。审查报告：`research/C_paper_full_review.md`；独立脚本：`src/audit_paper_evidence.py`；量化证据：`research/C_paper_review_evidence.json`。
- 确认Q4两套工作簿的24:00栏取成24:10 SOC，各79天受影响，最大849.622222 kWh。需修输出层及独立墙钟验证，暂不动全年调度；4小时位置分组也需人类确认墙钟对应。
- 支撑包缺 `data/raw/substitute/附件2.xlsx` 运行依赖。官网原始附件2含PV，现用52560个PV数值与官网完全一致；不是数据本身不一致。
- 分位候选同时上调PV和负荷，论文“只增加正常购电费/重复对冲”的解释与表中费用方向相反；不能据此证明稳健计划无效。
- 还需补Q2优化约束、Q2/Q3四个指定日期表、原始xlsx模板、完整代码附录、页码、真实AI使用说明及有效引用；Q1表有实测越界。
- 已只读复核四套全年SOC连接、原始供需缺口及Q2/Q4-2总费用；临时完整编译28页成功，未替换提交PDF，未重跑全年。后续按报告优先级修复再收口。
- 原有 `research/skills_shortlist.md` 暂存变更及 `skills/skills-candidates*` 未动。以下为原交接历史，不应把其中“只需提交”视为审查后结论。

## 当前结论

- 项目已选择C题，阶段为 `SOLVING`。
- 第四问已完成建模、全年正式回放、结果模板和独立核验。
- 第四问采用双价格通道：优化器使用决策时可得的预测价，执行与核算使用附件4真实目标槽价格。
- 价格预测仅用一月窗口选为同星期日四周衰减加权；2月至12月没有参与选模。
- 第二问式费用17743324.878930元；第三问式费用16606095.443703元；后者节省1137229.435226元（6.409337%）。
- 正式独立验证78/78通过，结果在 `outputs/q4/`，报告为 `research/C_Q4_validation_report.md`。
- 提交用 `支撑材料/` 和 `支撑材料.zip` 已重新生成（2026-09-13）。压缩包约5.74 MiB，包含60个Python源文件、5份正式结果、10个官方附件文件、6张论文图、《AI 工具使用详情.pdf》、运行说明和SHA-256清单（84条），未包含大体积中间结果。打包入口为 `src/package_support.py`。
- 论文参考文献已完成二次整理：10条文献均在正文对应位置实际引用。正文大章前的手工强制分页已取消，第三问结果表改为就近浮动，最终PDF为29页；标题衔接、参考文献和附录均已完成视觉检查，编译零横向越界。
- 已完成 2026 国赛 C 题公开解法联网对比，记录在 `外部解法对比/对比记录.md`。结论是第一问总费用与独立公开复现一致；第二至四问差异主要来自预测信息集、跨日 SOC 和问题四价格可知性，不能只按费用高低判断。公开图表中最值得借鉴的是问题二费用构成、典型日调度链和问题四预测价—真实价对照图，当前论文与正式结果未改动。

## 墙钟口径修复（2026-09-13）

审查发现正式结果工作簿的“24:00储电量”和六个四小时分块按槽位位置切块，而槽143实际位于次日00:00–00:10，导致墙钟语义错位。修复如下：

- 新增独立聚合层 `src/core/wallclock_output.py`，只按真实 `interval_start`/`interval_end` 归属自然日，并要求前一计划日槽143作为2月1日00:00–00:10的边界记录。
- 第二、三、四问回放器在评价期首日前一天额外产出 `*_wallclock_boundary.csv`；生成器与 finalize 脚本统一改用墙钟聚合，不再按位置切块。
- 五份正式产物已按修复后链路全部重算，费用与电量数值不变（Q2 16,886,077.669585；Q3 15,805,751.774901；Q4-2 17,743,324.88；Q4-3 16,606,095.44），Q4独立验证78/78、Q3独立验证、Q2三天窗口验收均通过。
- 论文第一问分块表保留题面“按位置归入”口径，并在正文明确标注块标签实际后移一个槽；第三问起的指定日期分块改用真实墙钟聚合。

## 复现链修复（2026-09-13）

- 官网完整附件2为 `附件2.xlsx`（同时含“小区负载”和“光伏发电实际功率”）；`data/raw/official/附件2.xlsm` 只有负荷表。原 `data/raw/official/` 缺少完整 xlsx，导致生产读取器直接报错。
- 读取器 `src/core/data_io.py` 的 `_resolve_official_attachment2` 自动在 official 目录定位含两张表的附件2，不再回退 `substitute/`。已核对官方xlsx光伏52560值与旧副本逐值一致（最大差0）。
- 补回 `data/raw/official/附件2.xlsx`；`src/generate_q2_result.py` 默认方法改为 `D-7`，与主报告口径一致。
- `src/generate_q3_result.py` 写缓存前自动创建 `experiments/`；README补充 `finalize_q3_result.py` 与 `extract_required_dates.py`。

## 重要修复（因果边界）

第四问接管时发现旧第二问、第三问七天视野把未来目标日当成未来决策日，会读取当前尚未发生的数据。D015已修复为整段视野共用当前真实决策时刻；历史反例与修复后参考对拍通过，新增测试6/6，原短测均回归通过。

该错误不影响第一问。第四问完全使用修复后接口。第二问、第三问的受控重算已完成：第二问修复后总费用 16,886,077.669585 元，已替换正式产物 `outputs/q2/`；第三问修复前后仅差浮点末位，正式工作簿不替换，只更新相对第二问的比较数字（节省 1,080,325.894684 元、6.397731%）。同星期日预测的因果冷启动已补齐并通过测试 10/10。

## 第四问关键文件

- 任务：`tasks/TASK_C_Q4_completion.md`
- 价格：`src/core/q4_price.py`、`src/core/q4_energy.py`
- 回放：`src/core/q4_replay.py`
- 正式生成：`src/generate_q4_results.py`
- 模板：`src/finalize_q4_results.py`
- 独立核验：`src/validate_q4_results.py`
- 完美信息对照：`src/q4_oracle_benchmark.py`
- 论文数字：`outputs/q4/q4_paper_tables.json`
- 论文：`paper/数模国赛C题.tex`

## 下一步

1. 参赛队员最终阅读论文与《AI 工具使用详情.pdf》，确认表述符合当届竞赛要求（尤其 R8 的AI使用范围需人工如实确认）。
2. 赛前按官方清单核对文件命名，并上传 `支撑材料.zip`。
3. 干净目录复现：解压 `支撑材料.zip`，将 `官方附件/`→`data/raw/official/`、`源码/`→`src/`，按 README 顺序运行生成与验证脚本。
4. 将当前支撑材料与状态同步修改提交；不要混入来源不明的工作区改动。

## 不要回退

- 不恢复“第四问尚未开始”或“N3仍阻塞”等旧状态。
- 不恢复旧的未来目标日决策时刻写法。
- 不恢复“按槽位位置切块”来填写结果工作簿的0:00/24:00储电量与四小时分块；正式工作簿必须走 `wallclock_output` 墙钟聚合。
- 不回退 `generate_q2_result.py` 的默认方法（应为 `D-7`）。
- 不修改D002、D003、D007、D011至D014已冻结的建模口径（论文正文已改用可读假设名，代码内注释仍可保留编号）。
- 不写成员个人路径，不覆盖 `data/raw/`。
