# C 题论文当前版本复核与修订清单

复核基线：2026-09-13，`正式建模` 分支，当前提交 `cfa4ae6`。本报告对原初稿审查逐条复核，只读检查论文、代码、正式结果和支撑材料；没有重跑全年优化、重选策略或修改正式数值。

## 总结

原审查总体可靠，但不能原样照搬：13项中，R1、R2、R3、R4、R6、R9、R10、R13仍成立；R5、R8、R11、R12部分成立；R7已经修复。

当前论文不需要推倒四问重建。最先处理正式输出语义、复现链、错误机制解释和官方格式合规；随后补齐模型表达、指定日期结果和收益归因；最后完成排版、图表和措辞增强。

优先级说明：P0为提交阻塞，P1影响结果正确性或核心论证，P2为证据及表达增强。凡涉及时间口径、模型重选和AI实际使用情况，必须由参赛队员确认，不由Agent猜测。

## 逐项复核

### R1 / P0：Q4 的24:00储电量存在墙钟错位——成立

- `src/core/slot_adapter.py` 定义槽143为次日 `[00:00,00:10)`。
- `src/finalize_q4_results.py` 仍按每24个位置槽分块，并把最后一槽的 `soc_end` 写入“24:00储电量”；这实际对应次日00:10。
- Q4-2、Q4-3均有79天受影响，最大差为849.622222 kWh。现有验证器使用相同分块语义，78/78不能发现共同口径错误。
- 处理：先由人类确认题表墙钟含义，再建立独立墙钟聚合层；不改主调度和费用。独立验收不得复用当前输出器的分块函数。

### R2 / P0：支撑材料不能按README独立复现——成立

- 主仓库和支撑材料代码仍从 `data/raw/substitute/附件2.xlsx` 读取光伏，而支撑包没有该相对路径；README只要求复制 `官方附件/`。
- 官方附件2当前实际包含负荷和光伏工作表，且已核对现用光伏数值与官方数据一致。因此这是依赖路径与打包问题，不是数据造假或全年数值失效。
- `generate_q3_result.py` 写缓存前未保证 `experiments/` 存在；README也漏列 `finalize_q3_result.py`。
- 当前工作区显示 `支撑材料.zip` 已删除，但 `STATE.md`、`SESSION_HANDOFF.md` 仍称其存在，提交状态不一致。
- 处理：统一读取官方附件的入口，补目录创建和完整生成顺序；从干净目录按README做最小复现并重新打包。

### R3 / P1：分位“保守计划”的方向与论文解释不一致——成立

- `src/core/q2_conservative.py` 同时给负荷和光伏加“实际值减预测值”的上分位残差。上调光伏会降低计划净负荷，不能解释为缺电风险意义上的单向保守。
- 论文仍写成“只上调负荷”，并把成本增加解释为只增加正常购电费；实际表格中正常费用下降，紧急购电量明显增加。
- 现有实验只能证明“已测试的联合分位候选表现较差”，不能推出合理设计的稳健计划无效。
- 处理：优先收窄命名、摘要和因果解释；如要保留强稳健性结论，再做负荷上调、光伏下调或净负荷上分位的小窗口对照。不得因此擅自重选冻结的D-7主策略。

### R4 / P1：问题二论文模型缺少完整优化约束——成立

- 七天滚动优化式只列SOC递推、储能边界和末端库存，没有完整写出预测负荷、光伏、购电、充放电之间的计划母线平衡及变量边界。
- 程序LP中相应约束存在，所以这是论文表达不闭合，不表示正式数值已经错误。
- 处理：补完整可行域，或明确引用问题一的物理约束并说明预测量替换；区分计划变量和实际执行变量，使读者不看代码也能还原有界LP。

### R5 / P1：缺少四个指定日期结果——部分成立

- 3月20日、6月21日、9月23日、12月21日的购电、分段充放电/SOC和紧急购电结果仍未在正文展示。
- 现有逐槽CSV已经包含数据，可自动抽取，无需重新优化；但必须先修正R1的墙钟聚合。
- 原审查中“后四个结果必须由xlsm改为xlsx”的判断证据不足：本地官方模板本身为xlsm，当前生成器也以其为底稿。额外工作表是否允许应按当届题目和提交系统确认，不能仅凭扩展名判错。

### R6 / P0：摘要页码和代码附录不符合格式要求——成立

- 当前PDF为24个物理页，不再是旧审查记录的28页。
- 摘要仍隐藏页码，正文才从1开始；附录只有文件清单，完整代码插入仍被注释。
- 2026格式规范要求摘要页作为第1页连续编号，并在附录列出支撑材料文件清单及完整可运行源程序。
- 处理：摘要页码已经改为从第1页连续编号。根据参赛队员最新明确指令，禁止再向代码附录增加内容，因此本轮不执行完整代码附录修复；该项保留为已知提交风险，最终由队员承担并确认。继续检查匿名信息、交叉引用、PDF大小及现有附录与支撑包一致性。

### R7：参考文献没有正文引用闭环——已修复

- 当前10条文献均在正文对应位置实际引用；没有 `nocite{*}`，没有未定义引用。
- 结论：本项关闭，只需在最终编译时保持引用检查。

### R8 / P1：AI使用披露可能不完整——部分成立，需人工确认

- 《AI工具使用详情》已经比初稿明显完整，覆盖题意理解、模型设计、程序实现、结果分析和论文整理。
- 正文仍只列Codex，并把用途写成代码调试、文档整理和文字润色，范围比支撑材料窄。
- 是否实际使用Claude Code、具体模型版本、使用时间和人工核验范围，只能由参赛队员如实确认；不能根据旧交接或审查意见直接补写。“全部人工复核”等强表述同样需要事实依据。
- 处理：由两名队员逐项确认工具、阶段、采纳修改和人工验证，再统一正文与支撑材料。

### R9 / P1：顺序预测无泄漏不等于独立样本外评价——成立

- D-7与同星期日方案仍按同一年度2—12月全年费用比较后选优，并报告胜出方案在同一评价期的费用。
- 每个决策时点没有使用未来真实量，只能说明预测执行层满足因果边界，不能使选模后的赢家费用成为独立外推估计。
- 处理：改称“同年度顺序回放比较”，同时报告候选结果，不声称已证明未来年度最优。Q4用一月选价格模型、2—12月评价的设计更干净，但不能自动消除Q2继承的选择偏差。

### R10 / P1：Q3总收益不能全部归因于日内更新——成立

- Q2总费16,886,077.669585元；Q3仅00:00更新总费16,350,694.803205元；Q3四时点总费15,805,751.774901元。
- Q2到Q3仅00:00降低535,382.866380元，其中包含预测来源变化；Q3仅00:00到四时点降低544,943.028304元，后者才是增加6:00、12:00、18:00更新的边际收益。两段合计1,080,325.894684元。
- 当前正文仍把全部108.03万元解释成“日内更新”收益，应拆分为两个证据台阶。
- Q4的完美信息下界已经明确同时使用未来真实负荷、光伏和电价，并称联合不可执行下界；这一部分目前基本合格，不应再批评为“纯价格价值”。

### R11 / P2：全年策略稳健性证据不足——部分成立

- 现有参数和输入扰动主要集中于问题一，不能证明问题二至四对预测偏差、终端条件和结算口径均稳健。
- 但当前论文已经明确“Q1输入扰动不能替代Q2—Q4全年顺序回测”，并把18:00结论限定在当前数据与附件给定时点，不再明显外推。
- 处理：时间允许时增加一个固定真实数据、初始SOC和评价器的短窗口控制变量检验；时间不足则维持当前收窄结论。本项是增强项，不是现有全年结果必然错误。

### R12 / P2：创新点应落在机制连接——部分成立

- 当前论文没有把LP、MPC、线性插值或D-7包装成首创，已经把价值放在信息发布时间、合同版本现金流、跨日库存和统一核算链上。
- 仍存在内部D007、D009编号和过度强调测试数量的问题；R10收益拆分和典型日机制图也尚未补齐。
- 处理：把内部编号改成明确假设名称，用机制图和收益分解说明组合设计实际改变了什么，减少“冻结”“若干项测试通过”等工程化表述。

### R13 / P1-P2：Q1表格仍越界，机制图仍不足——成立，但旧数值过时

- 当前编译日志中两张Q1表分别溢出约24.03 pt和41.97 pt，另有一处正文段落溢出约13.61 pt；位置在PDF物理第8页，而不是旧审查记录的页码和数值。
- 第二张表右侧“放电量(kWh)”及数值可见越过正文边界。此前分页任务只处理强制分页和Q3表浮动，没有解决Q1表宽。
- 当前只有4张图。最值得补的是一个典型日的实际净负荷、计划购电、紧急购电、SOC及0/6/12/18更新点联动图，其次是R10两台阶收益分解图。
- 部分图内英文标签与中文正文不统一。没有“必须达到某张数”的要求，不应为了数量增加装饰图。

## 已确认的正面证据

- 现用官方输入与官网附件的对应数据表逐行一致，光伏52,560个值最大差为0。
- 四套逐槽结果均为48,096槽、334天；相邻SOC最大断点为0 kWh。
- SOC递推最大误差约 `4.55e-12 kWh`，轨迹处于1200—10800 kWh；无显著同时充放电。
- 紧急电量可由原始供需与现有电池动作重构，最大差不超过 `3.56e-13 kWh`。
- Q2和Q4-2正式费用均可独立重算并与日表总和一致。

这些证据支持当前逐槽账本和费用核算具有可复用基础，但不能覆盖R1的输出时钟语义、R2的打包依赖、R3的错误机制解释以及官方格式问题。

## 分阶段修订顺序

### 验证体系和高级因素的统一原则

正文验证应贴近建模比赛的表达：先说明模型为什么可行、结果为什么可信，再给一两个独立对照，不用“19项、78项测试通过”代替数学论证。采用四层精简证据：

1. 逐槽供需平衡、SOC递推、边界和费用账本复算；
2. 问题一用双HiGHS或小规模KKT/强对偶、CBC核对最优性；
3. 与无储能或简单峰谷规则基线比较，说明优化的实际价值；
4. 问题二至四采用严格因果顺序回放和更新时点消融，解释预测信息的边际价值。

公开复现中的DP网格收敛可以作为外部旁证，但当前已有显式互斥MILP和双引擎对拍，不必为了方法数量重复实现。工作簿回读、散列和大量合成测试属于复现保障，放在附录或支撑材料，不在正文突出。

高级因素取舍如下：

- **电池寿命：可选敏感性，不进入正式目标。** 题目没有给出电池化学体系、循环寿命曲线、购置/更换成本和温度条件，直接加入寿命成本会引入无法校准的外部参数。正文说明这是局限；如时间允许，只做等效吞吐成本的参数敏感性。
- **SAA/CVaR：方法上合理，本轮不作为正式策略。** 它们适合处理负荷、光伏和电价不确定性，但需要严格因果的场景生成、概率权重、风险参数和未参与调参的评价期。当前数据和工期不足以证明其优于确定性滚动主线，可作为赛后统一信息集对照。
- **场景树：本轮无必要。** 它是多阶段随机规划的实现结构，不是单独的现实因素；分支数迅速增长，且当前附件只给四个确定发布时点。没有正式SAA/CVaR就不单独建立场景树。
- **MPC实时闭环：已有必要部分，不另建秒级模型。** 问题三已经在0:00、6:00、12:00、18:00获取新预报后锁定已执行时段并重算未来计划，本质上是题目粒度下的确定性MPC。真实设备秒级控制、逆变器动态和通信延迟没有数据支撑，只列为工程实施边界。

### 第一阶段：提交阻塞、结果语义与验证瘦身

1. 人工确认墙钟口径，修复R1聚合并独立验证。
2. 修复R2官方输入读取、目录创建、生成顺序和干净目录复现。
3. 修复R6摘要页码；按人类指令不扩充代码附录，并记录该已知格式风险。
4. 由队员确认R8的AI真实使用记录；此项可与前三项并行。
5. 建立“约束/账本复算—独立最优性核对—简单基线—顺序回放/消融”验证矩阵；正文删除以测试数量作为主要论据的写法。

### 第二阶段：核心论证与公开复现互补

1. 处理R3：先收窄错误结论；是否新增稳健候选实验由人类决定。
2. 补R4完整预测优化模型。
3. 重写R9评价边界和R10收益归因。
4. 在R1完成后抽取R5四个指定日期表。
5. 在问题一增加小规模KKT/强对偶或CBC独立核对；保留无储能/规则基线。DP只引用公开复现的收敛结果，不重复造一套网格程序。
6. 在模型局限中说明电池寿命参数缺失；将SAA/CVaR和场景树列为统一信息集下的后续风险模型，不写成本文成果；明确问题三已经实现题目粒度下的确定性MPC。

### 第三阶段：排版、图表、复现与最终验收

1. 修复R13两张Q1表和正文溢出。
2. 绘制典型日调度机制图和两台阶收益分解图。
3. 统一图表语言、单位、字号，删除内部决策编号和过度工程化表述。
4. 视时间决定是否追加R11短窗口稳健性检验。
5. 重新编译论文并逐页检查；确认页码、引用、附录、表格和图形无越界。
6. 从干净目录复现最小流程，独立回读五份正式结果。
7. 对照论文、正式结果、README、AI声明、文件清单和SHA-256清单。
8. 重新生成支撑材料压缩包，更新状态与交接；重大修改审查并由人类确认后再合并。

## 必须由人类确认的事项

1. 方案A特殊跨日槽下，题表0:00、24:00及四小时区间的最终墙钟解释。
2. 是否仅收窄R3结论，还是增加正确方向的稳健预测小实验。
3. AI工具、模型版本、时间、使用阶段以及人工核验的真实范围。
4. 官方xlsm模板、额外工作表及最终上传格式是否被当届系统接受。

官方依据：[2026论文格式规范](https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html)、[AI使用规范](https://en.mcm.edu.cn/html_en/node/532f38e63cfa84145697e2678575ba0a.html)。
